---
title: "CoRDE: Concept-Prior Routed Diffusion Experts for Structural Generalization in Robot Manipulation"
title_zh: CoRDE：概念先验路由扩散专家用于机器人操作中的结构泛化
authors: "Haidong Huang, Xixin Zhao, Yaohua Zhou, Jiayu Song, Jiayi Zhang, Jun Ma, Haiyue Zhu, Xiaocong Li"
date: 2026-06-20
pdf: "https://arxiv.org/pdf/2606.21935v1"
tags: ["query:moe-special"]
score: 9.0
evidence: 基于概念先验的路由避免扩散专家路由坍缩
tldr: 现有扩散模型在机器人模仿学习的多任务长时域场景中因结构泛化不足导致梯度冲突，纯数据驱动MoE易引发路由崩溃和参数爆炸。本文提出CoRDE，利用冻结概念编码器的语义先验通过软映射矩阵引导变分后验责任，并引入熵控制保持探索性，同时采用LoRA低秩适配共享主干实现参数高效。实验表明CoRDE显著减少路由崩溃，形成语义对齐的专家分配，动作质量与增量学习效率均优于基线，为机器人操作的结构泛化提供了有效范式。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.21935v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1653, \"height\": 871, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21935v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1860, \"height\": 942, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21935v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 902, \"height\": 379, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.21935v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1746, \"height\": 403, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.21935v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 661, \"height\": 181, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.21935v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1485, \"height\": 372, \"label\": \"Table\"}]"
motivation: 解决扩散模型在多任务长时域机器人模仿中结构泛化不足，以及纯数据驱动MoE的路由崩溃和参数膨胀问题。
method: 提出CoRDE，利用冻结概念编码器提取语义先验，通过可学习软映射矩阵引导变分后验责任，并用LoRA适配共享主干实现参数高效专家池。
result: 相比基线，CoRDE系统降低路由崩溃，形成鲁棒语义对齐专家分配，动作生成质量和增量学习效率显著提升。
conclusion: CoRDE通过概念先验引导与参数高效设计，有效增强机器人操作的结构泛化能力，理论分析支撑其生成保真度。
---

## 摘要
扩散模型在机器人模仿学习中擅长捕捉多模态动作分布。然而，在多任务和长时域场景中，单一架构缺乏结构泛化能力，在语义不同的子阶段之间会出现梯度冲突。纯数据驱动的混合专家（MoE）方法虽然引入了分工，但经常引发路由崩溃，并且实例化全规模专家会导致参数爆炸和高扩展成本。为了解决这些问题，我们提出了概念先验路由扩散专家（CoRDE），一种结构引导的变分蒸馏框架。CoRDE从冻结的概念编码器中提取语义分布，通过可学习的软映射矩阵引导变分后验责任。该机制引入了一个熵控制的责任推理过程，在可靠语义预测下鼓励自信路由，同时保留随机扩散项以实现行为多样性。为了克服参数膨胀，CoRDE采用参数高效专家池，在共享冻结骨干上使用低秩适应（LoRA）。理论分析表明，混合分数差异受责任加权局部专家误差的限制，支持低秩专家适应下的高保真生成。实证评估证实，与现有基线相比，CoRDE系统性地减少了路由崩溃，形成了稳健、语义对齐的专家分配，同时实现了优越的动作质量和增量学习效率。

## Abstract
Diffusion models excel at capturing multi-modal action distributions in robot imitation learning. However, in multi-task and long-horizon scenarios, monolithic architectures lack structural generalization capabilities, suffering from gradient conflicts between distinct semantic sub-stages. While pure data-driven Mixture-of-Experts (MoE) methods introduce labor division, they frequently trigger routing collapse, and instantiating full-scale experts causes parameter explosion and high expansion costs. To address these issues, we propose Concept-prior Routed Diffusion Experts (CoRDE), a structure-guided variational distillation framework. CoRDE extracts semantic distributions from a frozen concept encoder to guide the variational posterior responsibility via a learnable soft mapping matrix. This mechanism introduces an entropy-controlled responsibility inference process that encourages confident routing under reliable semantic predictions while preserving the stochastic diffusion term for behavioral diversity. To overcome parameter inflation, CoRDE employs a parameter-efficient expert pool using Low-Rank Adaptation (LoRA) on a shared frozen backbone. Theoretical analysis shows that the mixture score discrepancy is bounded by responsibility-weighted local expert errors, supporting high-fidelity generation under low-rank expert adaptation. Empirical evaluations confirm that, compared to existing baselines, CoRDE systematically reduces routing collapse, forming robust, semantically aligned expert allocations while achieving superior action quality and incremental learning efficiency.

---

## 论文详细总结（自动生成）

# 论文《CoRDE: Concept-Prior Routed Diffusion Experts for Structural Generalization in Robot Manipulation》详细总结

## 1. 论文的核心问题与整体含义

- **研究动机**：扩散模型在机器人模仿学习中能够捕捉多模态动作分布，但在多任务和长时域场景中，单一（monolithic）架构因不同语义子阶段之间存在梯度冲突而缺乏结构泛化能力。纯数据驱动的混合专家（MoE）方法虽引入分工，但常导致路由崩溃（routing collapse）和参数爆炸。
- **整体含义**：本文提出**CoRDE**（Concept-prior Routed Diffusion Experts），通过注入高层语义概念先验来引导扩散MoE的路由，解决结构泛化不足、路由不稳定和参数膨胀三大问题，实现鲁棒、可解释且参数高效的多任务机器人操作。

## 2. 论文提出的方法论

### 2.1 核心思想
- 将预训练的扩散模型蒸馏为**概念引导的MoE**：利用冻结的概念编码器提取稳定的语义分布，通过可学习的软映射矩阵（PCE）将其转化为结构先验，与行为证据融合得到变分后验责任（responsibility），从而指导专家分配。
- 采用**LoRA（低秩适应）**在共享冻结骨干上构建参数高效专家池，避免全参数专家带来的参数爆炸。
- 通过**双熵动力学**：最小化路由熵以保证宏观确定性，同时保留随机扩散项的全秩方差以维持行为多样性。

### 2.2 关键技术细节
1. **结构引导的责任推理**：
   - 冻结概念编码器输出语义分布 \( p_\phi(c|o) \)。
   - 软映射矩阵 \( P_{CE}(z|c) \) 将概念映射到专家，得到结构先验 \( p_{\text{struct}}(z|o) = \sum_c p_\phi(c|o) P_{CE}(z|c) \)。
   - 行为证据 \( r(z|o,a_0) \) 基于局部分数匹配误差。
   - 变分目标：\( \min_{\tilde{q}} \mathbb{E}[\text{KL}(\tilde{q}\|\bar{r}) + \lambda(o)\text{KL}(\tilde{q}\|p_{\text{struct}})] \)，最优后验为几何平均形式（式5），其中 \( \lambda(o) \) 根据概念置信度自适应调节。

2. **参数高效LoRA专家**：
   - 共享冻结的主干网络 \( h_\eta(o) \)，每个专家仅训练注入到线性投影层的LoRA模块 \( W^{(z)} = W_0 + B^{(z)}A^{(z)} \)。
   - 理论保证（Theorem 2）：混合分数场误差受局部专家误差的加权和上界，保证低秩适应下高保真生成。
   - 多样性保持：LoRA只改变确定性漂移项，随机扩散项（Wiener过程）保持全秩，避免模式坍塌。

3. **EM风格软对齐与路由器解耦**：
   - 路由器通过KL散度拟合后验责任，不与分数匹配任务梯度冲突。
   - 软映射矩阵通过狄利克雷平滑统计和指数移动平均（EMA）更新，建立可解释的概念-专家多对多映射。

### 2.3 算法流程
- **训练阶段（Phase I）**：离线变分蒸馏。采样小批量数据 → 提取语义先验 → 计算行为证据 → 推理后验责任（E步） → 更新LoRA专家（M步1） → 更新路由器（M步2） → 更新映射矩阵（M步3）。
- **推理阶段（Phase II）**：仅需观测，提取语义先验，路由器输出结合结构偏置，激活top-k专家，通过反向SDE生成动作序列。

## 3. 实验设计

### 3.1 数据集与场景
- **LIBERO**：四个标准套件：L-Spatial, L-Object, L-Goal, L-Long。用于评估跨套件的结构泛化能力和长时域多任务成功率。
- **D3IL**：7个任务（Avoiding, Aligning, Pushing, Stacking-1, Stacking-2, Sorting（图像）, Stacking（图像））。用于评估多模态行为多样性（Task Entropy）和推理效率。

### 3.2 对比方法
- **Diffusion Policy (Teacher)**：标准扩散策略。
- **Distill-MoE (Evidence-only)**：基于VDD的纯数据似然驱动的MoE蒸馏，无结构先验。
- **CoRDE (HiMaCon encoder)**：使用HiMaCon自监督概念编码器。
- **CoRDE (AutoCGP encoder)**：使用AutoCGP无监督概念编码器。
- 消融中还包括：全参数VDD、去掉PCE、去掉自适应λ、仅证据、无责任监督的路由器等变体。

### 3.3 公平性保证
所有蒸馏学生（Distill-MoE和两种CoRDE）共享相同的骨干网络、专家数量K、LoRA配置、训练步数，唯一变量是结构先验的集成。

## 4. 资源与算力

- **文中未明确说明**：没有提及使用的GPU型号、数量、训练时长等具体计算资源信息。仅提到“matched LoRA configurations”和“equivalent training steps”，但无定量数字。因此在资源与算力方面，论文信息不完整。

## 5. 实验数量与充分性

### 5.1 实验组数
- **主实验1**：LIBERO上4个套件 + 宏平均（共5个指标），对比4种方法。
- **主实验2**：D3IL上7个任务，对比8种方法，报告成功率和任务熵两列。
- **推理时间**：在state-based pushing任务上，对比NFE=1/8/32/64下的延迟，包括DP、VDD、CoRDE。
- **消融研究**：一个完整表格（Table III），包含6个变体（Full CoRDE、w/o PCE、w/o adaptive λ、Evidence-only、Router w/o responsibility supervision），在LIBERO Mean Success、D3IL Success、Task Entropy、Hroute、Hpost、PPL六个指标上对比。

### 5.2 充分性与客观性评价
- **充分性**：覆盖了多任务结构化泛化（LIBERO）和多模态多样性（D3IL）两个互补维度，消融实验系统性地检验了核心设计（PCE、自适应λ、责任监督等），且路由诊断指标（Hroute、Hpost、PPL）有助于区分结构化分工与路由崩溃。
- **客观性与公平性**：作者明确声明控制了参数预算和架构，使对比变量唯一。但在D3IL实验中，CoRDE（AutoCGP）部分任务上成功率甚至超过Teacher，可能存在随机波动或过拟合风险，但多次seed报告标准差，整体可信。未在真实机器人上验证，仅在仿真benchmark中评估，泛化性有待验证。

## 6. 论文的主要结论与发现

1. **性能提升**：CoRDE在LIBERO所有套件上显著优于Monolithic DP和Evidence-only MoE，尤其在强语义结构的L-Long和L-Goal套件中提升明显。
2. **路由稳定性**：CoRDE大幅降低路由熵（Hroute和Hpost更低），形成语义对齐的专家分配，避免路由崩溃（PPL远离1）。
3. **多样性保持**：在D3IL上，CoRDE的任务熵接近原始DP，远高于确定性基线，验证了随机扩散项对多模态行为多样性的保护作用。
4. **推理加速**：单步生成（NFE=1）仅2.63ms，比DP的55.62ms快一个数量级，且与VDD效率相当但动作质量更高。
5. **参数高效**：仅LoRA适配器即可达到甚至超越全参数VDD的性能，无参数爆炸问题。

## 7. 优点

- **方法创新性**：首次将高层概念先验与扩散MoE蒸馏有机结合，通过可学习软映射实现可解释的专家分工，避免了硬分配导致的决策突变。
- **理论严谨性**：提供了路由熵界（Theorem 1）和分数近似误差界（Theorem 2），从理论上支撑低秩专家的生成保真度。
- **参数效率**：LoRA专家池大幅降低存储和计算成本，便于扩展到更多专家。
- **实验全面性**：覆盖任务成功率、行为多样性、推理效率、路由诊断四个维度，消融研究设计细致，能够清晰归因每个模块的作用。
- **可解释性**：概念-专家映射矩阵提供语义层面的专家可解释性，有助于理解模型行为。

## 8. 不足与局限

- **实验覆盖有限**：仅在两个仿真benchmark（LIBERO、D3IL）上验证，缺乏真实机器人平台上的测试，也未涉及分布偏移或在线适应场景。
- **资源信息缺失**：未报告训练所需的GPU型号、数量、时长等算力信息，影响了可复现性评估。
- **概念编码器依赖**：方法性能受限于概念编码器质量，使用HiMaCon或AutoCGP虽无需人工标注，但若概念提取不准确（如语义模糊或特征混淆），可能干扰路由。消融中去除PCE后性能显著下降，表明对概念质量的敏感性。
- **专家数量与可扩展性**：实验未分析不同专家数量K对性能的影响，仅固定K值，结构泛化边界未充分探索。
- **路由偏置注入**：推理时对路由器logits施加固定系数β的结构偏置，β的取值未消融，可能不是最优。
- **未与近期强基线比较**：未对比如SMP、ForceVLA等其他MoE机器人方法，也未与一致性策略（Consistency Policy）等加速方法对比，对比基线以较老方法为主。

（完）
