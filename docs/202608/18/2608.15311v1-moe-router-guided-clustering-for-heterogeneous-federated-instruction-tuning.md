---
title: MoE Router-Guided Clustering for Heterogeneous Federated Instruction Tuning
title_zh: MoE路由引导的异构联邦指令微调聚类
authors: "Ankita Sharma, Bahar Farahani, Sanaz Rahimi Moosavi, Amir Rrahmani, Farshad Firouzi, Krishnendu Chakrabarty"
date: 2026-08-15
pdf: "https://arxiv.org/pdf/2608.15311v1"
tags: ["query:moe-special"]
score: 8.0
evidence: 利用MoE路由器的行为引导联邦指令微调中的客户端聚类
tldr: 联邦指令微调可在不共享数据下适应分散数据，但MoE模型在异质分布下聚合易导致负迁移。本文提出ClientMorpher，利用预训练MoE路由签名组织客户端协作，包含客户端中心与专家中心两种聚类策略。在Dolly-15K上测试，路由感知协作提升个性化性能且通信成本不变，为稀疏MoE联邦微调提供了可扩展方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.15311v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 888, \"height\": 359, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.15311v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1883, \"height\": 302, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.15311v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 874, \"height\": 352, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.15311v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 733, \"height\": 257, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.15311v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 931, \"height\": 196, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.15311v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1875, \"height\": 622, \"label\": \"Table\"}]"
motivation: 现有联邦MoE方法忽视路由行为，异质指令分布下 indiscriminate聚合导致负迁移，需识别应协作的客户端。
method: 提出ClientMorpher，利用MoE路由签名聚类客户端，含ClientMorpher-C（按专家激活剖面）与ClientMorpher-E（按专家交叉使用剖面）两种策略。
result: 在Dolly-15K上，路由感知协作相比联邦平均和本地训练获得更好个性化性能，同时保持相同通信成本。
conclusion: 客户端中心与专家中心聚类是稀疏MoE联邦指令微调的有效且可扩展方法。
---

## 摘要
联邦指令微调使得大型语言模型（LLMs）能够适应分散的、隐私敏感的数据，而无需数据共享。最近的混合专家（MoE）LLMs对于联邦学习特别有吸引力，因为它们的稀疏激活在扩展模型容量的同时减少了计算和通信开销。然而，现有的联邦MoE方法主要关注参数聚合和个性化，忽略了MoE模型的路由行为作为客户端协作信息来源的作用。在异构指令分布下，不加区分的聚合可能导致负迁移，因此需要识别联邦优化过程中哪些客户端应该协作。我们提出了ClientMorpher，一个路由感知的、个性化的联邦指令微调框架，利用预训练MoE模型的路由签名在聚合前组织客户端协作。我们研究了两种互补的聚类策略：ClientMorpher-C，直接使用专家激活特征对客户端进行聚类；以及ClientMorpher-E，首先基于跨客户端使用签名对专家进行聚类，然后推导出客户端协作组。我们在Databricks Dolly-15K数据集上评估了ClientMorpher用于联邦指令微调，在多个指令遵循任务中使用了病理性和基于Dirichlet的异构客户端分布。实验结果表明，与传统的联邦平均和本地训练相比，路由感知协作持续提高了个性化性能，同时保持了相同的通信成本。此外，我们的研究表明，以客户端为中心和以专家为中心的聚类为稀疏MoE LLMs的个性化联邦指令微调提供了一种有效且可扩展的方法。

## Abstract
Federated instruction fine-tuning enables Large Language Models (LLMs) to adapt to decentralized, privacy-sensitive data without requiring data sharing. Recent Mixture-of-Experts (MoE) LLMs are particularly attractive for federated learning because their sparse activation reduces computation and communication while scaling model capacity. However, existing federated MoE methods primarily focus on parameter aggregation and personalization, overlooking the routing behavior of MoE models as a source of information for client collaboration. Under heterogeneous instruction distributions, indiscriminate aggregation can lead to negative transfer, highlighting the need to identify which clients should collaborate during federated optimization. We propose ClientMorpher, a routing-aware, personalized federated instruction fine-tuning framework that leverages routing signatures from pretrained MoE models to organize client collaboration prior to aggregation. We investigate two complementary clustering strategies: ClientMorpher-C, which directly clusters clients using expert activation profiles, and ClientMorpher-E, which first clusters experts based on their cross-client usage signatures and then derives client collaboration groups. We evaluate ClientMorpher for federated instruction fine-tuning on the Databricks Dolly-15K dataset, using pathological and Dirichlet-based heterogeneous client distributions across multiple instruction-following tasks. Experimental results show that routing-aware collaboration consistently improves personalized performance compared to conventional federated averaging and local training, while maintaining the same communication cost. Furthermore, our study shows that client-centric and expert-centric clustering provides an effective and scalable approach for personalized federated instruction fine-tuning of sparse MoE LLMs.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义

**研究动机与背景：**

- 联邦学习（FL）允许多个客户端在不共享原始数据的前提下协作微调大型语言模型（LLM），满足隐私与专有数据约束。
- 混合专家（Mixture-of-Experts, MoE）模型因其稀疏激活性，在扩展模型容量的同时降低了计算与通信开销，成为联邦部署的理想基础架构。
- **现有联邦 MoE 方法的不足**：当前方法主要关注参数聚合与个性化（如 FedMoE、FedMoE-DA、pFedMoE 等），**忽视了 MoE 路由行为**作为一种客户端协作信息来源。
- **核心问题**：在异构指令分布下，客户端数据分布差异大，若不加区分地全局聚合（如 FedAvg），容易导致**负迁移**，降低模型个性化性能与整体质量。因此需要识别"哪些客户端应协作"这一问题。

## 2. 论文提出的方法论

**核心思想：**

> 预训练 MoE 模型的**路由行为**反映了客户端的数据分布特征——相似指令分布的客户端倾向于激活相似的专家集合。因此，利用**路由签名**在聚合之前对客户端进行聚类，可实现簇级个性化的联邦优化。

**框架总览（ClientMorpher）：**

- **预训练骨干冻结**：MoE 骨干与专家参数保持不变，仅优化少量 LoRA 适配参数。
- **Phase 0：共享客户端专家使用量画像**
  - 对每个客户端 i，用本地数据 Di 运行 B 个 batch 的冻结模型前向传播。
  - 统计每个专家被选入 top-k 的次数，得到专家使用计数向量 ci,e。
  - 归一化后得到客户端专家使用剖面向量 **u**i（公式 1-2），堆叠成客户端-专家矩阵 U ∈ [0,1]^(N×E)。

**两种聚类变体：**

| 变体 | 聚类对象 | 方法 |
|------|---------|------|
| ClientMorpher-C | 直接对客户端 | 对客户端剖面 {u_i} 进行 K-Means 聚类（公式 4） |
| ClientMorpher-E | 先对专家、再推导客户端 | 1. 对专家签名 s_e = U_{:,e} 归一化并 K-Means 聚类（公式 5-6）；2. 构建专家-簇隶属矩阵 M；3. 计算客户端对各专家簇的"路由质量" qi,k = Σ_e u_i,e · M_e,k，取最大者作为簇分配（公式 7） |

**共享训练阶段（Phase 2：簇级个性化联邦学习）：**

- 服务器为每个非空簇维护一个独立的适配状态 θ_a^(k)。
- 每轮通信中，簇内客户端接收对应状态，进行 L 步本地训练（公式 8），服务器按样本量加权聚合簇内更新（公式 9）。
- 整体流程见 Algorithm 1。

**两个变体的关系**：ClientMorpher-C 与 ClientMorpher-E 仅在聚类构建方式上不同，后续训练阶段完全一致。

## 3. 实验设计

**数据集与场景：**

- 使用 **Databricks Dolly-15K** 指令遵循数据集，涵盖 4 类任务：
  - CLS（文本分类）
  - QA（闭域问答）
  - IE（信息抽取）
  - Summ（摘要）
- 数据划分方式：
  - **Pathological 划分**：每个客户端仅拥有单一任务类别（4 客户端）。
  - **Dirichlet 划分**：α=0.1（高度异构）和 α=1.0（较均衡的 non-IID）。
- 训练格式：Alpaca 风格提示模板，损失仅在响应 token 上计算；评估时仅输入提示，生成响应与参考答案比较。

**评估指标**：ROUGE-L（越高越好）。

**对比方法：**

1. **Local Training**：仅本地微调 LoRA，无任何跨客户端通信。
2. **MoE-FedAvg**：标准 FedAvg，全局聚合所有客户端更新。
3. **ClientMorpher-C**：直接对客户端剖面聚类 + 簇级 FedAvg。
4. **ClientMorpher-E**：专家聚类 + 推导客户端分组 + 簇级 FedAvg。

**通信公平性**：所有联邦方法传输相同的 LoRA 参数，仅占全模型的 **0.0878%/轮**，确保性能差异归因于协作策略而非通信量不同。

## 4. 资源与算力

- **论文未明确说明**所使用的 GPU 型号、数量、分布式拓扑、训练时长等具体算力信息。
- 仅可推测：
  - 模型为预训练 MoE LLM + LoRA 微调（参数量约为主干的 0.0878%/轮）。
  - 客户端数量较少（4~8 个），整体实验规模相对较轻量。
- 由于论文省略了这些信息，无法从算力角度评估实验可复现性与资源门槛。

## 5. 实验数量与充分性

**已做实验：**

| 实验 | 内容 |
|------|------|
| 实验一（表 II） | 4 客户端 pathological 划分，对比 4 种方法，报告各任务与平均 ROUGE-L |
| 实验二（表 III） | 8 客户端 Dirichlet 划分（α=0.1 与 α=1.0），对比 ClientMorpher-C/‑E 与 Local/MoE-FedAvg，含按任务类别聚合的指标 |

**充分性分析：**

- ✅ **相对公平**：通信成本一致（0.0878%/轮），方法间对比客观。
- ✅ **异构覆盖**：覆盖病理划分和两种不同强度的 Dirichlet 划分，具有一定代表性。
- ⚠️ **缺少消融实验**：未探究聚类数 K 的影响、路由分析层数、LoRA 秩、本地步数等超参数敏感性。
- ⚠️ **基线较少**：未与更前沿的个性化联邦 MoE 方法（如 FedMoE-DA、pFedMoE、FLEx 等）直接对比。
- ⚠️ **模型单一**：未使用多个规模的 MoE 骨干或不同 MoE 路由配置。
- ⚠️ **未报告方差**：未说明多次随机种子实验的标准差，结论统计显著性存疑。

总体而言，实验设计基本合理且验证了核心假设，但规模与深度有限，只能视为初步验证。

## 6. 论文的主要结论与发现

1. **路由感知协作有效**：ClientMorpher 两种变体在大多数场景下优于 Local Training 和 MoE-FedAvg，且不增加通信成本。
2. **病理划分下（表 II）**：
   - ClientMorpher-E 平均 ROUGE-L 最高（0.4986），优于 MoE-FedAvg（0.4927）与 Local（0.4824）。
   - 在分类、闭域问答和摘要任务上最优；ClientMorpher-C 在信息抽取上最佳。
3. **Dirichlet 划分下（表 III）**：
   - α=0.1 时两者与 MoE-FedAvg 相当且优于 Local，ClientMorpher-C 在摘要上最优，ClientMorpher-E 在分类上更强。
   - α=1.0 时 ClientMorpher-E 平均最佳（0.4936），ClientMorpher-C 在摘要上仍略优。
4. **两种策略互补**：
   - 直接客户端聚类（ClientMorpher-C）在极端异构（路由模式分离明显）时更有效。
   - 专家中心聚类（ClientMorpher-E）在分布较均衡时产生更稳健的协作组。
5. **MoE 路由行为是有价值的个性化信号**：即使仅用轻量的路由统计信息，也能有效组织客户端协作。

## 7. 优点

- **方法新颖性强**：首次将 MoE 路由行为作为客户端相似性信号用于联邦指令微调，角度独特。
- **轻量高效**：路由画像只需冻结模型前向传播，不增加通信开销；训练仅传输 0.0878% 参数。
- **两种互补视角**：客户端中心与专家中心聚类在概念上对称，且实验验证其互补性，设计具有一定理论美感。
- **隐私友好**：只交换路由统计信息（或更准确地说，仅用该信息做聚类），不直接暴露原始数据。
- **框架简洁可扩展**：聚类与训练解耦，可适配任意稀疏 MoE LLM 和个性化 FL 框架。
- **公平的实验对比**：所有联邦方法使用相同通信预算，隔离了通信量的干扰变量。

## 8. 不足与局限

- **缺少算力与实现细节**：未报告 GPU 型号、训练时间、模型规模、联邦轮数 R、本地步数 L、聚类数 K 等关键实验配置，**可复现性不足**。
- **实验规模有限**：客户端数量少（4~8），任务类型仅 4 类，数据集仅 Dolly-15K，无法判断在更大规模、更复杂场景中的表现。
- **无统计可靠性分析**：未报告多次运行的均值和方差，也未做显著性检验，结论稳健性存疑。
- **缺少消融研究**：未评估聚类数 K 的敏感性、路由层数的影响、画像 batch 数 B 的作用等。
- **未与前沿个性化联邦 MoE 方法对比**：实验仅对标 Local 与 MoE-FedAvg，缺少对 FedMoE-DA、pFedMoE、FLEx 等最新方法的横向比较。
- **假设的局限**：方法依赖预训练 MoE 路由的区分度；若预训练路由本身不具任务区分性（相关的"路由反映几何而非专业领域"观点），方法效果可能受限。
- **应用限制**：方法针对稀疏 MoE 模型设计，对稠密 LLM 不适用；也未讨论客户端动态加入/退出、客户端数量变化等真实场景。

（完）
