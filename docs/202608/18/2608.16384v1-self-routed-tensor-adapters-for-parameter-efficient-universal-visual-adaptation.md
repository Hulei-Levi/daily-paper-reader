---
title: Self-Routed Tensor Adapters for Parameter-Efficient Universal Visual Adaptation
title_zh: 自路由张量适配器用于参数高效通用视觉适应
authors: Suraj Yadav
date: 2026-08-17
pdf: "https://arxiv.org/pdf/2608.16384v1"
tags: ["query:moe-special"]
score: 8.0
evidence: 自路由张量适配器通过多专家通路提升专业化，并摆脱外部路由和大专家库
tldr: 视觉基础模型跨域适配常因固定低秩子空间而受限，MoE方法又依赖额外路由与大规模专家库。本文提出自路由张量适配器SRTA，在低秩空间中仅用可学习域矩阵计算路由权重，并融合共享Tucker核切片生成样本级适配矩阵，无需外部门控。在五个多域分类基准上，SRTA以远少于MoLoRA的参数取得相当或更优精度，兼顾共享知识与域特化，实现高效通用视觉适配。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.16384v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1246, \"height\": 676, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.16384v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1262, \"height\": 276, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.16384v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1181, \"height\": 345, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.16384v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1184, \"height\": 398, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.16384v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1097, \"height\": 184, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.16384v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1258, \"height\": 970, \"label\": \"Table\"}]"
motivation: 固定低秩子空间难以适应风格、背景等异构域，而MoE路由与适配分离且参数开销大。
method: 将输入映射到低秩空间，用可学习域矩阵计算路由权重，加权融合共享Tucker核切片，并加入深度加权路由监督。
result: 在五个多域视觉基准上，SRTA精度与MoE式PEFT相当或略优，4域仅2.77M参数，远低于MoLoRA的9.52M。
conclusion: SRTA以紧凑参数实现共享视觉因素复用与域感知特化，提供精度-参数均衡的通用多域适配方案。
---

## 摘要
通用视觉表示需要适应机制，能够在异构域之间进行适应，而不会将知识碎片化为特定领域的模块。参数高效微调能够高效地适应冻结的视觉基础模型，但标准的低秩适配器对所有输入使用固定的子空间，当域在风格、背景和语义上下文上存在差异时，这可能会受到限制。基于MoE的适配器通过多条专家路径提高了专门化能力，但通常依赖外部路由器和大型专家库，增加了参数并将路由与适应分离。我们提出了**自路由张量适配器**，一种用于多域视觉适应的紧凑框架。SRTA将每个输入投影到低秩空间，使用可学习的域矩阵从该表示计算路由权重，并利用这些权重混合共享Tucker核心的切片。这产生了样本特定的适应矩阵，无需外部门控网络，允许重用共享视觉因子，同时支持域感知的专门化。为了加强路径学习，我们引入了渐进式深度加权路由目标，跨适配器层监督路由决策。在五个异构多域视觉分类基准上，SRTA实现了与MoE风格PEFT基线相当或略高的平均准确率，同时使用的可训练参数显著更少。在秩为64时，SRTA在4域设置中使用2.77M参数，而MoLoRA为9.52M；在6域设置中使用3.00M参数，而MoLoRA为14.31M。总体而言，SRTA为将视觉基础模型适应为通用多域表示提供了有效的准确率-参数权衡。\href{https://github.com/surajyadav-research/SRTA}{GitHub}

## Abstract
Universal visual representations require adaptation mechanisms that adapt across heterogeneous domains without fragmenting knowledge into domain-specific modules. Parameter-efficient fine-tuning adapts frozen visual foundation models efficiently, but standard low-rank adapters use a fixed subspace for all inputs, which can be restrictive when domains differ in style, background, and semantic context. MoE-based adapters improve specialization through multiple expert pathways, but often rely on external routers and large expert banks, adding parameters and separating routing from adaptation. We propose \textbf{Self-Routed Tensor Adapters}, a compact framework for multi-domain visual adaptation. SRTA projects each input into a low-rank space, computes routing weights from this representation using a learnable domain matrix, and uses these weights to blend slices of a shared Tucker core. This produces a sample-specific adaptation matrix without an external gating network, allowing shared visual factors to be reused while supporting domain-aware specialization. To strengthen pathway learning, we introduce a progressive depth-weighted routing objective that supervises routing decisions across adapter layers. Across five heterogeneous multi-domain visual classification benchmarks, SRTA achieves competitive or slightly stronger average accuracy than MoE-style PEFT baselines while using substantially fewer trainable parameters. At rank 64, SRTA uses 2.77M parameters in the 4-domain setting compared with 9.52M for MoLoRA, and 3.00M in the 6-domain setting compared with 14.31M. Overall, SRTA offers an effective accuracy-parameter trade-off for adapting visual foundation models toward universal multi-domain representations. \href{https://github.com/surajyadav-research/SRTA}{GitHub}

---

## 论文详细总结（自动生成）

## 论文详细中文总结

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：视觉基础模型（如ViT）在跨异构域（风格、背景、采集条件、语义上下文不同）进行多域适配时，面临两难困境：
  - 传统PEFT方法（如LoRA）对所有输入使用固定的低秩子空间，在异构多域场景下过于局限，可能导致负迁移和域间干扰。
  - MoE风格的适配器（如MoLoRA、MOELoRA）虽通过多专家路径提升了特化能力，但通常依赖外部路由网络和大型专家库，引入额外参数，且将路由决策与适配表示解耦，可能导致冗余专家、路由不稳定和共享效率低下。
- **整体含义**：论文主张将路由机制融入适配器自身表示中，用共享的张量分解结构组织多域知识，实现“自我路由”的紧凑适配，从而在共享视觉因子和域感知特化之间取得平衡，以更少的参数实现MoE级别的多域适应性能。

---

### 2. 论文提出的方法论

**核心思想**：将适配更新空间构造为Tucker分解的张量结构，路由概率直接由适配器的低秩输入表示与可学习域坐标矩阵的交互产生，无需外部路由网络。

**关键技术细节**：
- **四个可训练组件**：
  - 输入投影矩阵 V ∈ R^{d_in × r1}
  - 输出投影矩阵 U ∈ R^{r2 × d_out}
  - 共享Tucker核心张量 G ∈ R^{r3 × r1 × r2}
  - 域坐标矩阵 C ∈ R^{r1 × r3}（r3 = 域数）
- **自路由流程**：
  1. 输入 x 投影到低秩空间：z = xV
  2. 剔除[CLS] token后，对patch token做平均池化得到路由签名 z_route
  3. 路由logits：q = z_route · C^T / τ（τ为温度参数）
  4. 通过softmax获得路由概率 α
  5. 用α动态加权融合Tucker核心切片，构造样本特定核心矩阵 Σ_b = Σ_t α_{b,t} G_t
  6. 适配输出：y_adapt = (z_b · Σ_b) · U，最终输出 y = xW₀ + s·y_adapt
- **渐进式深度加权路由监督**：对各适配器层的路由logits施加辅助交叉熵损失，权重 w_ℓ = ℓ/M（浅层权重小、深层权重大），直接用域标签监督中间层路由决策；推理时不需要域标签。
- **最终训练目标**：
  L_total = L_CE(y_pred, y*) + λ_route · (1/M) · Σ_ℓ w_ℓ · L_route^(ℓ)

---

### 3. 实验设计

- **数据集/基准**：5个广泛使用的多域视觉分类基准——PACS、VLCS、Office-Home、Digits-DG（各4个域）和NICO++（6个域）。
- **数据划分**：每个基准80/20训练-测试划分；从训练部分均匀采样2400张图（2000训练/400验证），保证小数据量下的公平评估。
- **骨干网络**：冻结的vit-base-patch16-224-in21k，仅训练适配模块和分类头。
- **对比方法**：
  - 静态PEFT：LoRA、DoRA
  - MoE-PEFT：MALoRA、MOELoRA、MoLoRA
  - SRTA（本文方法）
- **默认配置**：适配器注入所有Transformer层的Query和Value矩阵；rank 64（r1=r2=64，r3=域数）；30轮训练、batch size 64、lr 5×10⁻⁴、weight decay 0.01、warmup 0.1；τ=0.1，s=4.0，λ_route=1。

---

### 4. 资源与算力

- **论文中关于算力的信息有限**：仅在Acknowledgement部分提到GPU计算支持由IIIT-Delhi的Infosys Center for Artificial Intelligence提供，但未明确说明GPU型号、数量、训练时长或总计算量（如GPU-hours）。读者无法据此精确评估训练成本。

---

### 5. 实验数量与充分性

- **实验数量**：
  - 主结果：5个数据集上的平均精度对比（表1）。
  - 参数效率：4域/6域、4种rank配置的参数对比（表2）。
  - 损失组件消融：λ_route = 0 vs 1.0（表3）。
  - 秩消融：5种方法、多种rank配置（表4），覆盖面广。
  - 路由行为分析：5个数据集的平均路由概率热力图（图2）。
- **充分性与客观性**：
  - 实验覆盖了多类PEFT方法、多个秩、多个域数，比较全面。
  - 统一数据划分、统一骨干、统一训练超参，保证了对比的公平性。
  - 多次运行（mean ± std）报告结果，增强了可信度。
  - 不足：仅限图像分类任务，未覆盖分割、检测、视频等；路由监督需已知域标签，对无标签域场景未做实验。

---

### 6. 论文的主要结论与发现

- SRTA在5个多域基准上平均精度达88.1%，略优于最强的MoE基线MoLoRA（88.0%），接近Office-Home上MoLoRA的最佳成绩，在PACS和VLCS上取得最佳。
- 在rank 64下，SRTA在4域设置仅需2.77M参数（约MoLoRA的1/3.4），6域设置仅3.00M参数（约MoLoRA的1/4.8），显著更省参数。
- 引入渐进式深度加权路由监督可将平均精度从87.1%提至88.1%，对域间重叠较大的数据集（VLCS、Office-Home、NICO++）提升尤为明显。
- 路由行为分析显示：视觉差异大的域（PACS、Digits-DG）路由分布尖锐、近对角；语义重叠多的域（Office-Home、NICO++）路由更柔和、共享更多核切片，说明SRTA确实学习了可解释的域感知路径。

---

### 7. 优点

- **方法设计亮点**：
  - 路由与适配在同一低秩表示空间内完成，实现“自路由”，消除外部路由网络带来的解耦问题。
  - Tucker分解结构以紧凑方式同时支持共享表示与域特化，替代大专家库。
  - 渐进式深度加权路由监督给中间层提供直接学习信号且不增加推理开销。
  - 推理时不需域标签，具备实用性。
- **实验设计亮点**：
  - 对比方法覆盖静态PEFT和MoE-PEFT的多种代表。
  - 同时报告精度和参数数，强调精度-参数权衡。
  - 消融实验针对关键组件（路由损失）和关键超参（rank）设计合理。

---

### 8. 不足与局限

- **任务覆盖有限**：仅验证了图像分类，未涉及检测、分割、视频理解、多模态等更广泛视觉任务。
- **依赖域标签的监督**：路由监督需要已知域标签；真实场景中域可能未知、动态变化，论文未探索无监督或自适应路由机制。
- **精度提升幅度有限**：与MoLoRA相比平均精度仅提高0.1%，核心优势主要在参数效率而非准确率大幅领先；Office-Home上仍略逊于MoLoRA。
- **规模与算力细节缺失**：未报告具体GPU配置和训练时间，难以复现成本评估。
- **特定基准的小型数据集**：训练数据仅2000张，虽保证公平性，但与真实大规模数据场景存在差距。
- **单作者、单机构研究**：缺少更广泛的独立复现验证。

---

（完）
