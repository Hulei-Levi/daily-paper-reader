---
title: "Beyond the Previous Layer: Residual Predictive Structure in Sparse MoE Routing"
title_zh: 超越上一层：稀疏 MoE 路由中的残差预测结构
authors: "Hao Li, Yasuyuki Tahara, Yuichi Sei"
date: 2026-09-15
pdf: "https://arxiv.org/pdf/2609.17940v1"
tags: ["query:moe-special"]
score: 9.0
evidence: 利用冻结OLMoE和JetMoE分析稀疏MoE路由中的残差预测结构
tldr: 稀疏MoE逐token选择专家，但仅用上一层选择未必能概括路由轨迹。研究冻结OLMoE与JetMoE，保留最近选择为基线，检验更早专家选择对下一路由的预测增益。OLMoE历史从1层扩至11层，路由器logit R²由0.59879升至0.66544；JetMoE四层增益显著且通过非线性解码与参数匹配控制。结果表明专家选择轨迹存在超越相邻层持续性的残差预测结构。
source: arxiv
selection_source: fresh_fetch
tables_json: "[{\"url\": \"assets/tables/arxiv/2609.17940v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 699, \"height\": 755, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.17940v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 660, \"height\": 641, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.17940v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1211, \"height\": 504, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.17940v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1407, \"height\": 699, \"label\": \"Table\"}]"
motivation: 稀疏MoE路由通常只依赖相邻层选择，但该假设是否充分尚不明确，需检验历史专家选择能否提供额外预测信息。
method: 冻结OLMoE与JetMoE，以最近一层选择为基线，用线性探针、MLP、交叉拟合残差和参数匹配控制，比较加入更早专家选择后的路由器logit预测增益。
result: OLMoE历史从1层扩至11层使R²从0.59879升至0.66544；JetMoE四层增益0.14275与0.20528，非线性解码和残差预测也显著。
conclusion: 专家选择轨迹包含超越相邻层持续性的残差预测结构，表明路由预测应纳入更早历史而非仅依赖上一层。
---

## 摘要
稀疏专家混合模型将每个 token 依次路由经过一系列专家选择。我们探究紧邻的前一次选择是否足以概括该轨迹，以预测下一个路由器。使用冻结的 OLMoE 和 JetMoE 模型，我们以最近一次专家选择作为共同基线，测量来自更早专家选择的留出预测增益。在 OLMoE 中，将历史从一层扩展到十一层，使路由器 logit 的 R^2 从 0.59879 提升至 0.66544。一项预注册的 JetMoE 复制实验在两个目标深度上分别得到 0.14275 和 0.20528 的四层增益，配对 bootstrap 区间均高于零。这些增益在非线性解码下依然存在：将历史加入一个小型多层感知机可使 R^2 分别提升 0.17137 和 0.21861，而仅对最近状态进行非线性解码相较于线性探针仅提升 0.00139 和 0.00936。参数匹配的对照实验保持了该优势，且交叉拟合的历史残差能以 0.20549 和 0.23556 的 R^2 预测目标残差。这些发现识别出专家选择轨迹中超越相邻层持续性的残差预测结构。

## Abstract
Sparse mixture-of-experts models route each token through a sequence of expert selections. We ask whether the immediately preceding selection adequately summarizes this trajectory for predicting the next router. Using frozen OLMoE and JetMoE models, we measure the held-out predictive gain from earlier expert selections while retaining the most recent selection as a common baseline. In OLMoE, extending the history from one to eleven layers raises router-logit $R^2$ from 0.59879 to 0.66544. A preregistered JetMoE replication yields four-layer gains of 0.14275 and 0.20528 at two target depths, with paired bootstrap intervals above zero. These gains survive nonlinear decoding: adding history to a small multilayer perceptron improves $R^2$ by 0.17137 and 0.21861, whereas nonlinear decoding of the recent state alone adds 0.00139 and 0.00936 over a linear probe. Parameter-matched controls preserve the advantage, and cross-fitted history residuals predict target residuals with $R^2$ of 0.20549 and 0.23556. These findings identify residual predictive structure in expert-selection trajectories beyond adjacent-layer persistence.

---

## 论文详细总结（自动生成）

## 1. 核心问题与整体含义

- **研究动机**：稀疏 MoE 中，每个 token 会沿模型深度形成一条“专家选择轨迹”。一个自然假设是：要预测下一层 router，紧邻的上一层专家选择已经提供了足够信息。
- **核心问题**：在显式保留“最近一层选择”作为共同基线后，更早层的专家选择是否仍能提供额外的留出预测增益？也就是说，路由轨迹中是否存在超越“相邻层持续性”的历史预测结构。
- **整体含义**：论文试图区分两种现象：一种是相邻层之间本来就存在的路由持续性；另一种是更早历史选择带来的增量价值。作者称之为“残差预测结构”。
- **背景关联**：跨层路由结构已推动 Read-ME、RMoE、PathMoE、共享路由几何/动态分析、MoE 强化学习中的专家路径回放、HeRo 历史感知路由等工作。本文关注的是：在已有预训练稀疏 MoE 中，旧选择相对于最近选择到底还有多少预测价值。
- **主要贡献**：
  - 评估路由历史相对于显式“上一层基线”的增量价值；
  - 在 OLMoE 与 JetMoE 两个预训练 MoE 架构上验证，并包含预注册复制实验；
  - 使用非线性解码、参数匹配控制、交叉拟合残差预测和置换控制，说明历史增益并非线性探针伪影。

## 2. 方法论

- **状态、目标与比较**：
  - 对固定 token，设 \(S_l\) 为第 \(l\) 层被选专家的二元向量，\(g_l\) 为该层原生 router logit 向量。
  - “历史”指同一 token 沿模型深度的前序层，而不是序列中更早的 token。
  - 探针 \(F_k\) 用最近 \(k\) 层选择 \(S_{l-k:l-1}\) 预测目标层 logit \(g_l\)：
    \[
    R_l(k)=R^2_{\text{TEST}}(F_k(S_{l-k:l-1}), g_l)
    \]
  - 历史增益定义为：
    \[
    \Delta_l(k)=R_l(k)-R_l(1)
    \]
    其中 \(k=1\) 是只使用上一层的基线，\(\Delta_l(k)>0\) 表示更早选择带来额外预测价值。
  - \(R^2\) 是各坐标分数的均匀平均；目标 logit 在每个样本内中心化，并使用 FIT 统计量标准化。

- **冻结评估与解码控制**：
  - 所有 backbone 冻结、非量化，以 FP16 评估。
  - OLMoE 选 8/64 个 MLP 专家；JetMoE 选 2/8 个专家。
  - 线性探针使用 ridge regression，\(\alpha=1\)。
  - 所有 scaler 和 PCA 变换只在 FIT 上拟合。
  - EIPC 使用 64 个 PCA 分量；EPD 使用 32 个分量；RMO 中最近状态与历史窗口分别做 16 分量 PCA，使每个 ridge 输入为 32 维；JetMoE RMC 中最近选择保持 8 维，历史用 8 个 PCA 分量，总共 16 维。

- **非线性解码 NHD**：
  - 记最近状态 \(R=S_{l-1}\)，较老历史 \(H=S_{l-4:l-2}\)，目标 \(Y=g_l\)。
  - 比较仅用 \(R\) 的预测器与使用 \([H;R]\) 的预测器。
  - 最近状态有 8 个原始二元坐标，历史有 24 个坐标，NHD 不使用 PCA。
  - 两个 MLP 均含一个 32 单元 GELU 隐藏层。
  - 参数匹配控制：最近状态专用 MLP 为 77 单元，参数数 1,317，与历史模型 1,320 相差不超过 3 个参数。
  - 训练使用 full-batch AdamW，学习率 \(10^{-3}\)，权重衰减 \(10^{-4}\)，最多 300 epoch，验证 patience 为 30；FIT 内固定 384/128 划分选 checkpoint；初始化种子为 42、123、2026。

- **残差预测与置换控制**：
  - 分别训练 MLP 从 \(R\) 预测 \(Y\) 和从 \(R\) 预测 \(H\)。
  - 残差为：
    \[
    \epsilon_Y=Y-f(R),\quad \epsilon_H
