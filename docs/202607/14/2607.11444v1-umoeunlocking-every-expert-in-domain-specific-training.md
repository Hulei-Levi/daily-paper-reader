---
title: "UMoE:Unlocking Every Expert in Domain-Specific Training"
title_zh: UMoE：在领域特定训练中解锁每个专家
authors: "Xuefeng Li, Pengfei Liu"
date: 2026-07-13
pdf: "https://arxiv.org/pdf/2607.11444v1"
tags: ["query:moe-special"]
score: 9.0
evidence: 剪枝并重新生长MoE专家以实现领域专业化
tldr: MoE模型在领域专用后训练中，专家池由混合域预训练形成，大量专家对目标域贡献小。UMoE提出预算不变的三步流水线：先剪枝低领域对齐重要性的专家，再通过基于扰动的专家扩展恢复原规模，最后进行标准SFT。在Qwen3-30B-A3B和Qwen3.5-35B-A3B上跨数学、代码等五个领域12个基准测试一致提升，数学平均准确率+3.4，SWE-bench+6.0。该方法将冗余专家容量转化为有效领域容量，降低训练损失且增益随数据增长持续。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.11444v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1377, \"height\": 756, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.11444v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1623, \"height\": 425, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.11444v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1625, \"height\": 562, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.11444v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1621, \"height\": 559, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.11444v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 764, \"height\": 655, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.11444v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 769, \"height\": 655, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.11444v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1188, \"height\": 282, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.11444v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1644, \"height\": 599, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.11444v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1635, \"height\": 319, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.11444v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1068, \"height\": 425, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.11444v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1644, \"height\": 364, \"label\": \"Table\"}]"
motivation: MoE模型领域后训练中专家池未针对目标域优化，大量专家贡献微弱，标准SFT无法改变专家组成。
method: 先按领域对齐重要性剪枝低贡献专家，再通过扰动实现专家扩展恢复原规模，最后执行标准SFT，保持参数和推理成本不变。
result: 在两种MoE架构、五个领域12个基准上一致提升，数学平均准确率+3.4，SWE-bench+6.0，且数据扩展实验显示增益持续。
conclusion: UMoE重新分配专家容量，将冗余专家转化为有用领域容量，降低训练损失，提升所有难度级别性能，无需领域调参。
---

## 摘要
混合专家（MoE）模型以不成比例的计算成本扩展容量，已成为前沿大语言模型（LLM）的关键架构。然而，领域特定的后训练继承了由混合领域预训练形成的专家池：大量子集专家在目标领域贡献甚微，而标准的监督微调（SFT）保持该池的组成不变。我们提出了一种简单的、保持预算的流水线，在微调前将专家池重新对齐到目标领域。给定目标领域，我们（1）剪枝领域对齐显著性最低的专家，（2）通过基于扰动的专家扩展将专家池恢复到原始大小，（3）应用标准SFT。所得模型保留原始专家数量、参数数量和推理成本。使用单一固定配方且无需针对每个领域调整超参数，UMoE在两种MoE架构（Qwen3-30B-A3B和Qwen3.5-35B-A3B）、五个领域（数学、代码、科学、工具使用和智能体编码）以及12个基准测试中持续优于直接SFT。代表性改进包括数学平均准确率提升3.4个百分点，SWE-bench Verified提升6.0个百分点。在强内部数学语料上，直接SFT已超越Qwen3-30B-A3B-Thinking（82.81 vs. 81.06），而UMoE进一步将平均值提升至84.17，额外增加1.36个百分点，展示了在显著更强的SFT机制下的鲁棒性。数据扩展实验进一步表明，随着训练数据增长，增益持续存在。分析显示，直接SFT模型将大量路由专家计算分配给低显著性子集，而这些子集事后去除后平均性能下降很小；UMoE将这种冗余容量转化为有用的领域容量，实现更低的训练损失，并在下游评估的所有难度级别上获得收益。

## Abstract
Mixture-of-Experts (MoE) models scale capacity without proportional compute cost and have become a key architecture for frontier large language models (LLMs). Yet domain-specific post-training inherits an expert pool shaped by mixed-domain pre-training: a substantial subset of experts contributes little on the target domain, and standard supervised fine-tuning (SFT) leaves the composition of this pool unchanged. We propose a simple, budget-preserving pipeline that realigns the expert pool to the target domain before fine-tuning. Given a target domain, we (1) prune the experts with lowest domain-aligned saliency, (2) regrow the expert pool to its original size through perturbation-based expert expansion, and (3) apply standard SFT. The resulting model preserves the original expert count, parameter count, and inference cost. With a single frozen recipe and no per-domain hyperparameter tuning, UMoE consistently improves over direct sft across two MoE architectures (Qwen3-30B-A3B and Qwen3.5-35B-A3B), five domains (math, code, science, tool-use, and agentic coding), and 12 benchmarks. Representative improvements are 3.4 points in math average accuracy, 6.0 points on SWE-bench Verified. On a strong in-house math corpus, direct sft already surpasses Qwen3-30B-A3B-Thinking (82.81 vs.\ 81.06), yet UMoE further raises the average to 84.17, an additional 1.36 points, demonstrating robustness to a substantially stronger SFT regime. Data-scaling experiments further show that the gain persists as training data grows. Analysis reveals that the direct-SFT model allocates substantial routed-expert compute to a low-saliency subset that can be removed post hoc with little average degradation; UMoE turns this redundant capacity into useful domain capacity and achieves lower training loss, with gains spanning all difficulty levels in downstream evaluation.

---

## 论文详细总结（自动生成）

# UMoE：在领域特定训练中解锁每个专家 —— 详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究背景**：混合专家（MoE）模型通过稀疏路由实现容量扩展而不成比例增加计算成本，已成为前沿大语言模型（如Qwen3.5、DeepSeek-V4）的核心架构。领域特定后训练旨在将通用MoE适配到目标领域，但预训练形成的专家池中，大量专家在目标域上贡献微弱，存在 **“低显著性子集”** —— 这些专家仍接收非零路由，但路由计算被浪费在冗余功能上。
- **核心问题**：标准监督微调（SFT）直接在原有专家池上更新参数，**无法显式重新分配专家插槽**，导致固定容量中部分专家长期与目标域对齐不佳，限制了领域能力的提升。
- **整体含义**：能否在SFT之前**重新组织专家池**，使其初始化为更对齐目标域的状态？作者提出UMoE，在不改变模型架构、参数数量、推理成本的前提下，通过剪枝+扩展操作，将冗余专家容量转化为有用的领域容量，从而释放被锁定的专家潜力。

## 2. 论文提出的方法论：核心思想、关键技术细节

**核心思想**：在领域SFT之前，对预训练MoE的专家池进行**领域感知的重新组织**，使更多专家插槽从领域对齐的初始化状态开始学习。

**三步骤流程**（算法1概括）：

1. **领域对齐专家剪枝**（§3.2）
   - 使用**REAP显著性度量**：对于每个专家 \( e \) 在层 \( l \)，计算其在校准集上被选中的令牌上，**门控权重加权后的MLP输出L2范数的条件均值**。
   - 每层保留显著性最高的 **50%** 专家（\( E/2 \)），移除其余专家。

2. **预算保持的专家扩展**（§3.3）
   - 从保留的 \( E/2 \) 个专家出发，**通过扰动初始化**生成新的专家，恢复原始数量 \( E \)。
   - **分配策略**：每个保留专家（父本）通过**轮询**方式生成一个自由插槽的新专家（子本）。
   - **对称扰动**：对父本和子本的MLP权重和路由器行分别独立添加高斯噪声：\( W' = W + \sigma \cdot s(W) \cdot \epsilon \)，其中 \( \sigma = 0.05 \)，\( s(W) \) 为该参数组的经验标准差。
   - 扰动必须**同时作用于两侧**（而非仅新专家），且**同时扰动MLP和路由器行**（二者提供互补的对称性）。

3. **领域SFT**：对重新组织后的模型在所有参数上进行标准自回归交叉熵微调，使用与直接SFT完全相同的超参数和数据。

**关键设计原则**：
- 剪枝使用REAP而非单纯路由频率（频率已很强，但REAP略优）。
- 扩展必须对称扰动两侧，避免不对称导致路由退化。
- 扰动组合（MLP+路由器）联合使用最优。

## 3. 实验设计

| 维度 | 具体设置 |
|------|----------|
| **模型架构** | Qwen3-30B-A3B（128路由专家，K=8，无共享专家）；Qwen3.5-35B-A3B（256路由专家，K=8，有共享专家） |
| **领域与SFT数据** | 数学（OpenMathReasoning 300k；内部语料~150k）、代码（OpenCodeReasoning 300k）、科学（Nemotron-SFT-Science-v2 696k）、工具使用（Toucan 119k）、智能体编码（OpenSWE 12k） |
| **评估基准** | 数学：AIME 2024/2025/2026、HMMT Feb/Nov 2025、Feb 2026（共6个比赛）；代码：LiveCodeBench v5 (279题)、v6 (131题)；科学：GPQA-Diamond；工具使用：τ-bench、τ²-bench（航空+零售平均）；智能体编码：SWE-bench Verified (500实例) |
| **对比方法** | 主要与**Direct SFT**（在同一数据上直接微调原模型）对比；另在内部数学数据上与Qwen3-30B-A3B-Thinking、SU-01、Nex-N2-Mini、Qwen3.6-35B-A3B等外部模型比较 |
| **评估指标** | 数学：avg@32（32样本平均准确率）；代码/科学/工具：avg@8；SWE-bench：pass@1 |
| **消融实验**（§5.1） | 剪枝标准对比（EAN均值、频率、REAP）；扩展方式对比（单侧噪声vs对称；仅路由器/仅MLP/两者组合） |

## 4. 资源与算力

- 论文中**未明确说明**所使用的GPU型号、数量、训练时长，也未报告训练总FLOPS或能耗。
- 训练细节：使用slime框架，Adam优化器（β₁=0.9, β₂=0.95, weight decay 0.1），学习率cosine调度（峰值1e-4→1e-5），global batch size 512，所有SFT训练1 epoch。
- 推测需要较大GPU集群（如A100-80G或H100），但作者未披露。

## 5. 实验数量与充分性

- **总数**：在2种架构、5个领域、12个基准测试上进行了主实验；加上**数据尺度实验**（3个领域、多个数据量，共15个配对比较）、**消融实验**（8个变体）、**后验剪枝分析**、**路由动态分析**，总计实验组数十组以上。
- **客观性**：所有领域使用同一套超参数（“frozen recipe”），无领域特定调参，降低过拟合风险。每个领域采用公开或建设性数据集，评估基准为广泛接受的标准化测试。
- **公平性**：UMoE与Direct SFT共享完全相同的SFT数据、超参数和学习步数，唯一差异仅在于专家初始化状态。外部模型对比时，使用相同评估协议（max length 102400等）。
- **充分性评估**：实验设计较为充分，覆盖了推理（数学、代码、科学）和智能体（工具、编码）两类任务，并验证了数据扩展下的稳健性。但**每个领域仅使用一种SFT数据集**，可能不足以代表该领域的全部多样性；此外，仅在两种架构上验证（Qwen系列），缺少在其他MoE架构（如DeepSeek、Mixtral）上的实验。

## 6. 论文的主要结论与发现

1. **性能领先**：在12个基准测试中，UMoE全部优于Direct SFT。代表性提升：数学平均+3.4 (Qwen3-30B-A3B)、SWE-bench +6.0个点、Qwen3.5-35B-A3B上+1.67。在强内部数学数据（Direct SFT已超越Qwen3-30B-A3B-Thinking）上，UMoE仍额外提升+1.36。
2. **数据尺度鲁棒**：从0.5B到4.1B tokens，所有15个比较均偏向UMoE，增益未随数据量增加而消失。
3. **冗余机制揭示**：Direct SFT将约42%的路由计算分配给低显著性子集，而该子集可在后验剪枝后几乎无性能损失（平均仅-0.12）。UMoE使更大比例的专家成为非冗余（剪枝损失-5.0），将冗余容量转化为有效容量。
4. **训练动态更优**：UMoE虽初始损失稍高，但几步内反超直SFT，最终训练损失更低（0.396 vs. 0.404）。
5. **增益覆盖全难度**：问题层面的通过率分析显示，在所有难度区间（按Direct SFT通过率分箱）UMoE均优于Direct SFT，中间段提升最显著（9-11点）。

## 7. 优点

- **预算不变**：不改变参数量、专家数量、活跃专家数和推理成本，可直接替换原模型。
- **跨领域零调参**：所有领域使用同一套超参数和配方，无需人工干预，实用性强。
- **设计简洁有效**：剪枝+扰动扩展两步操作，无辅助损失、无额外训练阶段，易于复现。
- **机制分析深入**：通过路由-显著性不匹配、后验剪枝、训练损失曲线等多角度解释，论证了冗余容量被重新利用。
- **代码和智能体领域验证**：在更复杂的工具使用和智能体编码任务上也取得显著提升（SWE-bench +6.0），表明方法不仅适用于纯推理。

## 8. 不足与局限

- **架构通用性未验证**：仅测试了Qwen系列两个变体，未在DeepSeek-MoE、Mixtral等其他流行MoE架构上验证。
- **剪枝比例固定为50%**：未探索不同剪枝比例对效果的影响，自适应剪枝比例可能更优。
- **扩展策略局限**：使用简单的高斯扰动，可能不是最优的初始化策略；未尝试基于梯度或更复杂的专家合成方法。
- **单领域单数据集**：每个领域仅使用一种SFT数据集，可能引入数据偏见；真实领域内数据分布差异大时，效果是否稳定未知。
- **资源算力未报告**：无法评估方法训练成本，也缺乏计算效率的定量比较。
- **智能体评估局限性**：SWE-bench使用特定scaffold（SWE-agent），结果可能依赖代理框架，非纯模型能力测试。
- **安全性/公平性未讨论**：领域专业化可能导致知识遗忘或偏差放大，论文未分析。

（完）
