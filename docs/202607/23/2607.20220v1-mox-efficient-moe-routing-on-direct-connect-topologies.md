---
title: "MoX: Efficient MoE Routing on Direct-Connect Topologies"
title_zh: MoX：直接连接拓扑上的高效MoE路由
authors: "Ori Cohen, Jakob Krebs, Daniel Amir, Mark Silberstein"
date: 2026-07-22
pdf: "https://arxiv.org/pdf/2607.20220v1"
tags: ["query:moe-special"]
score: 9.0
evidence: 直连拓扑上的MoE路由
tldr: "MoE模型引入运行时稀疏动态流量，传统方案需动态重配置拓扑。MoX通过离线优化静态路由，构建token感知多播树并求解受限多播树打包问题平衡流量。在1024-TPU Boardfly拓扑上加速MoE块达1.8倍，减少瓶颈负载47%，在随机扩展拓扑中接近理想分组交换性能。证明无需动态重配置即可在静态直连拓扑上高效训练推理MoE。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.20220v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 832, \"height\": 448, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.20220v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1825, \"height\": 527, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.20220v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 882, \"height\": 296, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.20220v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 890, \"height\": 432, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.20220v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 877, \"height\": 347, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.20220v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 879, \"height\": 384, \"label\": \"Figure\"}]"
motivation: MoE模型的动态稀疏流量使传统动态路由或拓扑重构效率低下，需设计静态路由方案。
method: 构建token感知多播树减少带宽代价，通过求解受限多播树打包问题预计算静态链路权重平衡流量。
result: "在Boardfly拓扑上加速MoE块1.8倍，瓶颈负载降低47%，随机扩展拓扑中接近理想性能。"
conclusion: 静态负载感知路由能实现直连拓扑上的高性能MoE，无需动态需求驱动重配置。
---

## 摘要
光交换网络适用于密集ML模型的规律性通信，但MoE引入了稀疏的、运行时依赖的流量。我们证明，高效的离线优化路由能够在直接连接拓扑上实现高效的MoE训练和推理，而无需MoE流量矩阵或动态拓扑重配置。MoX构建令牌感知的多播树以减少带宽开销，然后通过求解受限多播树打包问题，使用静态、预计算的链路权重来平衡流量。利用大型MoE模型的记录流量、令牌级轨迹和ASTRA-sim，我们发现MoX在完整的MoE块——分发、专家计算和合并——上比最小跳数路由加速高达1.8倍。此外，它在随机扩展器拓扑中达到了近乎理想的包交换网络性能。在谷歌Boardfly拓扑的1024-TPU模型上，MoX将分发瓶颈链路负载降低了高达47%。这些结果表明，通过优化的负载无关路由，无需按需重配置即可在静态直接连接结构上实现高性能MoE。

## Abstract
Optically switched networks suit the regular communication of dense ML models, but MoE introduces sparse, runtime-dependent traffic. We show that efficient offline-optimized routing enables efficient MoE training and inference on direct-connect topologies without the need for MoE traffic matrix or dynamic topology reconfiguration. MoX constructs token-aware multicast trees to reduce bandwidth tax, then uses static, precomputed link weights to balance traffic by solving a restricted multicast tree-packing problem. Using recorded traffic from large MoE models, token-level traces, and ASTRA-sim, we find that MoX accelerates the full MoE block -- dispatch, expert computation, and combine -- by up to 1.8x over min-hop routing. Moreover, it attains nearly ideal packet-switched network performance in random expander topologies. On a 1,024-TPU model of Google's Boardfly topology, MoX reduces the dispatch bottleneck link load by up to 47%. These results show that high-performance MoE on static direct-connect fabrics can be achieved via optimized load-oblivious routing without demand-driven reconfiguration.

---

## 论文详细总结（自动生成）

# MoX：直接连接拓扑上的高效MoE路由——详细总结

## 一、论文的核心问题与整体含义（研究动机和背景）

- **问题**：MoE（混合专家）模型在训练和推理时产生稀疏、运行时依赖的流量，这与密集模型规律性的通信模式不同。直接连接拓扑（如光交换网络）虽然适合规律通信，但MoE流量的动态性和偏斜导致传统路由（如最小跳数）效率低下，主要表现为**带宽税**（中继浪费带宽）和**负载不平衡**。
- **背景**：专家域迅速增长（如DeepSeek-V3使用256专家，top-k=8），低度直接连接拓扑无法提供单跳连接，而光交换机重配置速度慢，无法跟上MoE每次迭代的流量变化。现有方案要么动态重配置拓扑（代价高），要么混合电交换（如MixNet），但后者限制了光网络的效用。
- **核心动机**：论证无需动态重配置或预测流量矩阵，仅通过离线优化的静态路由即可在直接连接拓扑上实现接近理想交换机的MoE性能。

## 二、论文提出的方法论：核心思想、关键技术细节

### 核心思想
1. **令牌感知多播树**：利用MoE分发阶段每个令牌需多播到K个专家的事实，构造多播树，使中继节点本身也是目标专家，从而避免纯中继的带宽税。
2. **静态负载平衡路由**：离线预计算每个物理链路的一个权重，在线通过加权轮询选择中继，无需感知当前流量矩阵或偏斜。

### 关键技术细节
- **多播树构造**（§2.1）：
  - 源节点s，目标集D（K个专家所在加速器）。
  - 维护持有令牌的节点集H，初始H={s}。
  - 优先选择路径中所有节点都属于D的路径（即利用已持有令牌的目标节点作为中继），仅当无此类路径时才允许非目标中继（产生一次带宽税）。
  - 通过位图（bitmap）在包头部编码剩余目标，实现无状态多播。
- **负载平衡权重**（§2.2）：
  - 每个有向链路关联一个权重w(r,d)。
  - 对于候选中继集C（当前H中与目标d相邻的节点），MoX按权重比例分配流量（式1），在线通过加权轮询计数器实现。
- **离线权重计算**（§2.4）：
  - 采用蒙特卡洛采样，对每个源节点生成1000个令牌，随机选择top-K专家组合（均匀假设）。
  - 初始权重为1，模拟后更新：过载链路降低权重（式6：log w ← log w - η(Le/mean(L)-1)），迭代多次，保留最低最大链路负载的权重。
- **合并阶段**（§2.5）：反转多播树，在中间节点进行部分归约，减少带宽占用。

### 公式与算法流程（文字说明）
- 目标：最小化最大链路负载（即式3-5的线性规划代理）。
- 流程：采样 → 模拟路由 → 计算每链路负载 → 权重更新 → 重模拟（共6次迭代）。最终权重在运行期固定。

## 三、实验设计：数据集、场景、基准、对比方法

- **数据集/场景**：
  - 真实MoE负载：DeepSeek-V3（256专家）和Qwen-3 225B（128专家）的Chakra执行轨迹，记录最后几轮训练的专家分配和流量。
  - 合成负载：Zipf分布模拟偏斜，覆盖不同系数变差。
  - 拓扑：随机度8扩展器（16/32/64节点）、Google TPUv8i Boardfly（1024 TPU）。
  - 训练批次：每GPU 5120个BF16 token（宽度7168）；推理：64/128/256 token/GPU。
- **基准**：理想全连接交换机（Switch）。
- **对比方法**：
  - 最小跳数路由（Min-Hop）。
  - 拓扑优化方法：基于MixNet思想，先根据流量矩阵调整拓扑（增加直接连接），再使用Min-Hop路由。
  - 组合方法：拓扑优化 + MoX路由。
  - 消融：带宽税单独效果（仅多播树而不做权重平衡）。
- **模拟器**：ASTRA-sim 2，使用token级EP Chakra轨迹，模拟每链路传输。

## 四、资源与算力

- **论文未明确说明具体GPU型号、数量或训练时长**。仅提到在真实GPU集群上记录DeepSeek-V3和Qwen-3的运行痕迹，以及使用ASTRA-sim进行模拟。
- 离线权重计算在单CPU核上几秒内完成（n=64），无需GPU。
- 评估基于模拟，可能对应NVIDIA或TPU环境，但未提供硬件细节。

## 五、实验数量与充分性

### 实验组数
- **端到端MoE块时间**：3种拓扑规模（16/32/64节点）× 3种top-k（8/10/12） × 训练和推理（3种推理batch），共约（3×3×4=36个数据点），每个点报告与理想交换机的归一化时间（图2）。
- **偏斜敏感度**：用Zipf合成数据对比Switch、MoX、Min-Hop、拓扑优化、拓扑优化+MoX共5种方法，在16节点上top-k=8，覆盖多种偏斜（图4）。
- **Boardfly瓶颈分析**：训练和推理下比较Min-Hop、带宽税（仅多播树）、MoX三种，归一化最大物理链路负载（图5）。
- **消融分析**：分离多播树贡献和权重贡献（图6a）；节点度影响（图6b）。
- **理论代理验证**：分析最大链路负载与端到端时间的相关性（图3）。

### 充分性评价
- **充分**：覆盖不同拓扑规模、不同top-k、训练/推理、多种偏斜程度，并与多种对比方法比较。
- **客观公平**：所有比较在相同网络带宽（800Gbps拆分为8×100Gbps）和相同负载下进行；Boardfly使用真实拓扑参数；控制变量良好。
- **弱项**：未与最新的动态重配置方案（如Photonic Rails）直接对比端到端时间；Boardfly实验仅分析了瓶颈负载代理，未做完整端到端模拟；仅使用两种真实MoE模型（虽具代表性但数量较少）。

## 六、论文的主要结论与发现

1. **性能提升显著**：MoX在完整MoE块上比最小跳数路由加速最高1.8倍（训练16节点top-12），且更接近理想交换机。
2. **接近理想性能**：在随机扩展器上，训练时64节点top-12下MoX仅比理想交换机慢7.3%，而最小跳数慢96%；推理256 token时慢13.6% vs 98%。
3. **优于拓扑优化**：在偏斜负载下，MoX比基于流量矩阵的拓扑优化方法快27%，且结合两者无额外收益。
4. **Boardfly适用性**：在1296-TPU Boardfly上，MoX将最大瓶颈链路负载降低47%（其中多播树贡献33%，权重贡献14%），证明对规则但非对称拓扑也有效。
5. **离线优化足够**：静态、负载无关的路由无需运行时重配置即可达到优秀性能，消除了对动态拓扑重构的依赖。

## 七、优点：方法或实验设计上的亮点

- **方法创新性**：首次将多播树和离线权重优化结合应用于MoE路由，避免了复杂的流量预测或拓扑重配置。
- **轻量实用**：离线计算仅需秒级，在线状态仅需每链路一个权重，令牌级处理开销低（头部位图仅8字节对比14KB嵌入）。
- **普适性**：可应用于任意直接连接拓扑（不规则扩展器、规则高密度拓扑如Boardfly）。
- **实验设计周密**：
  - 采用真实MoE负载轨迹，模拟了实际训练中的偏斜和动态变化。
  - 使用理论代理（最大链路负载）与模拟验证高度一致（图3），证明瓶颈分析的有效性。
  - 消融实验清晰分离了多播树和权重优化的各自贡献。
- **对比全面**：不仅对比最小跳数，还对比了动态拓扑优化方法，且分析了对偏斜的鲁棒性。

## 八、不足与局限

### 实验覆盖方面
- **模型和负载代表性有限**：仅基于DeepSeek-V3和Qwen-3，未涵盖更大规模（如Kimi K3 896专家）或其他架构的MoE模型。
- **Boardfly未做端到端模拟**：只报告了链路负载代理，缺少实际通信时间对比，结论强度略降。
- **缺少硬件实测**：所有结果出自ASTRA-sim模拟，未在真实光学交换机或NVLink集群上验证，可能忽略延迟、拥塞控制等细节。
- **未考虑故障容错**：静态路由若某链路故障需重新计算权重，文中未讨论。

### 潜在偏差风险
- **均匀路由假设**：离线优化基于均匀专家组合采样，若实际负载存在严重偏斜且层间专家映射不随机，效果可能下降（论文承认但依赖辅助损失和随机映射来减轻）。
- **多播树构造对非均匀度拓扑的适应性**：文中未分析在非度8拓扑（如度4）下的表现，可能受限。

### 应用限制
- **通信粒度**：要求每令牌单独路由并携带位图，当令牌数量极大（如推理批处理大）时头部开销仍可忽略，但若嵌入尺寸小（如稀疏激活）则开销比上升。
- **需要修改运行时系统**：需支持每令牌转发和部分归约，当前框架（如NCCL）不支持，需额外实现。
- **不能完全避免带宽税**：在少数情况下仍需非目标中继，且随拓扑增大开销增加（图6a中64节点开销7%）。

（完）
