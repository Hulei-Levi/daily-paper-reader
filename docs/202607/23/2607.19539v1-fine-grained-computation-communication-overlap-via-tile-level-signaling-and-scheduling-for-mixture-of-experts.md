---
title: Fine-grained Computation-Communication Overlap via Tile-level Signaling and Scheduling for Mixture-of-Experts
title_zh: 基于分片级信令与调度的细粒度计算-通信重叠用于混合专家模型
authors: "Minyu Cui, Anna Wingkvist, Morgan Ericsson"
date: 2026-07-21
pdf: "https://arxiv.org/pdf/2607.19539v1"
tags: ["query:moe-special"]
score: 9.0
evidence: 通过tile级调度优化MoE通信
tldr: MoE架构的分布式部署存在all-to-all通信延迟问题。本文提出细粒度计算-通信重叠方法，通过持久化计算与通信内核协同设计，利用tile级信号调度实现专家计算与返回all-to-all重叠。在4-A100上实现最高2.64倍端到端加速，且无需修改底层算子。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.19539v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 842, \"height\": 218, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.19539v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 801, \"height\": 1044, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.19539v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 788, \"height\": 339, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.19539v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 814, \"height\": 406, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.19539v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 863, \"height\": 430, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.19539v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 880, \"height\": 905, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.19539v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 879, \"height\": 900, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.19539v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1799, \"height\": 471, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.19539v1/fig-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 1802, \"height\": 446, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.19539v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 890, \"height\": 199, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.19539v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 873, \"height\": 491, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.19539v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 690, \"height\": 258, \"label\": \"Table\"}]"
motivation: 传统MoE实现将返回all-to-all置于专家计算之后，暴露通信延迟，降低GPU利用率。
method: 设计持久化计算内核覆盖所有本地专家并优先远程关键tile，同时在一个SM分区上运行持久化通信内核按段粒度发送就绪tile。
result: 在4-A100上对三种MoE模型评估，相比四个SOTA系统实现最高2.64倍端到端加速和2.74倍MoE层加速。
conclusion: 该方法在保持正确性的前提下，通过非侵入式协同设计有效提升多GPU系统上分布式MoE执行效率。
---

## 摘要
混合专家（MoE）架构在不按比例增加计算成本的情况下提升了模型容量，已成为将大型语言模型（LLM）扩展到万亿参数规模的关键构建模块。高效部署这些MoE模型依赖于跨多个GPU的分布式执行，其中每个MoE层涉及两次全对全通信：将令牌分派到专家秩，并将专家输出返回到其源秩。传统的MoE实现在专家计算完成后启动返回全对全，导致通信延迟暴露在关键路径上，降低GPU利用率。我们提出了一种细粒度方法，通过分片级信令和调度，将专家计算与第二次全对全重叠。我们的生产者-消费者协同设计结合了：（1）持久化的每个秩计算内核（生产者），覆盖该秩上的所有本地专家，以消除重复的内核启动开销，并优先处理远程关键分片；（2）在流式多处理器（SM）的一个小型专用分区上运行的持久化通信内核（消费者），一旦分片就绪即发起段粒度传输。我们的协同设计避免了对底层计算算子或通信原语的侵入性更改，使其在改进多GPU系统上分布式MoE执行效率方面具有实用性。在4块A100 GPU平台上，针对三个MoE模型与四个最先进的MoE系统进行评估，我们的方法实现了高达2.64倍的端到端加速和2.74倍的MoE层加速。与传统的无重叠基线相比，我们的方法在不同GEMM形状、路由器模式以及广泛的生产者/消费者SM分区下，持续提升算子和MoE层级的性能，同时保持正确性。

## Abstract
Mixture-of-Experts (MoE) architectures increase model capacity without proportionally increasing computation cost and have become a key building block for scaling large language models (LLMs) to trillion-parameter regimes. Efficient deployment of these MoE models relies on distributed execution across multiple GPUs, where each MoE layer involves two all-to-all communications: dispatching tokens to expert ranks and returning the expert outputs to their source ranks. Conventional MoE implementations launch this return all-to-all after expert compute completes, exposing communication latency on the critical path and reducing GPU utilization. We present a fine-grained approach that overlaps expert compute with the second all-to-all via tile-level signaling and scheduling. Our producer-consumer co-design combines: (1) a persistent per-rank computation kernel (producer) that covers all local experts on the rank to eliminate repeated kernel launch overhead and prioritizes remote-critical tiles, and (2) a persistent communication kernel (consumer) on a small dedicated partition of streaming multiprocessors (SMs) that issues segment-granular transfers as tiles become ready. Our co-design avoids intrusive changes to the underlying computation operators or communication primitives, making it practical for improving distributed MoE execution efficiency on multi-GPU systems. On a 4-A100 GPU platform, evaluated on three MoE models against four state-of-the-art MoE systems, our approach achieves up to 2.64x end-to-end speedup and 2.74x MoE-layer speedup. Compared with a conventional non-overlap baseline, our approach consistently improves both operator- and MoE-layer-level performance across varying GEMM shapes, router modes, and a broad range of producer/consumer SM partitions, while preserving correctness.

---

## 论文详细总结（自动生成）

# 论文总结：基于分片级信令与调度的细粒度计算-通信重叠用于混合专家模型

## 1. 核心问题与整体含义（研究动机和背景）

- **问题**：混合专家（MoE）架构在分布式部署时，每个MoE层需要两次all-to-all通信（令牌分发和专家输出返回）。传统实现将返回all-to-all置于专家计算之后，导致通信延迟暴露在关键路径上，GPU利用率低下。
- **背景**：GPU计算吞吐增长速度远超互联带宽，使得通信开销占比越来越大。现有分解方法和融合方法存在粗粒度、主机同步开销大或工程复杂性高等问题。
- **意义**：提出一种细粒度的计算-通信重叠方案，通过分片（tile）级信令和调度，在不修改底层算子和通信库的前提下，有效隐藏返回all-to-all的延迟，提升分布式MoE执行效率。

## 2. 论文提出的方法论

- **核心思想**：生产者-消费者协同设计，将专家GEMM计算（生产者）与返回all-to-all通信（消费者）重叠。两个持久化内核运行在GPU上，通过设备端分片级信号协调，无需主机参与。
- **关键技术细节**：
  - **远程拥有者对齐的行布局**：将每个本地专家的输入行按原始拥有者rank分组，远程行在前、本地行在后，并在owner边界对齐到分片高度（tile height），使得每个输出分片只属于一个目标rank，便于通信。
  - **远程优先的分片调度**：优先调度远程分片（需要通信）而非本地分片，且专家负载大的优先，最大化通信窗口。
  - **持久化生产者内核**：一个rank只启动一个持久化GEMM内核，覆盖所有本地专家，消除重复启动开销；内部按上述调度顺序计算，并在每个分片完成时通过信号标记就绪。
  - **持久化消费者内核**：运行在少量SM上（可调），轮询分片就绪信号，以段（segment）粒度发起NVSHMEM远程写入。段大小可调（通常合并多个row band以提高带宽利用率），首末段保留为单band以减少延迟。
  - **SM分区与流优先级**：生产者和消费者内核运行在不同CUDA流上，消费者拥有更高优先级，并独占部分SM，避免资源干扰。
- **算法流程**（文字描述）：
  1. 预处理阶段：根据路由信息构建远程拥有者对齐的行布局，生成分片调度表（远程优先）和组合计划（预计算每个分片的目标rank、偏移量、有效行数）。
  2. 执行阶段：启动持久化生产者GEMM内核和持久化消费者通信内核。生产者按调度计算每个分片，写入全局内存后通过原子操作发出信号。消费者轮询信号，一旦某段（一个或多个row band）的所有分片就绪，即以一个连续远程写入发送给目标rank。
  3. 生产者完成所有分片后，消费者继续处理剩余就绪段，完成后结束。

## 3. 实验设计

- **数据集/场景**：基于三种MoE模型（M-GPT、M-BERT、M-Trans-xl），均从标准Transformer衍生，分别代表稀疏替换、间隔替换和密集替换场景。此外还使用单个MoE层微基准测试（改变专家数量、GEMM形状、路由器模式等）。
- **Benchmark**：端到端前向延迟、MoE层延迟、吞吐量、重叠比例等。
- **对比方法**：四个最先进的MoE系统——FasterMoE、Tutel、Megatron-CUTLASS、Megatron-TE，以及一个传统无重叠基线（使用cuBLAS和NCCL的串行实现）。
- **参数扫描**：包括tokens per rank（8192/16384）、输出隐藏维N（4096/8192）、专家数E（4/8/16/32/64）、路由器模式（balanced / moderate_skew / stress_skew）、消费者CTA数量（2~24）、中间段合并行数（1或2）。

## 4. 资源与算力

- **GPU**：单个节点4块NVIDIA A100 GPU，通过NVLink互联（每通道25 GB/s单向，一对GPU 4条通道，约100 GB/s单向带宽）。每GPU 108个SM，40 GB HBM。
- **软件**：CUDA 12.1、NCCL 2.29.3、PyTorch 2.6.0、CUTLASS 3.9、NVSHMEM 3.6.5。
- **训练时长**：论文未明确说明训练时长，实验为推理（前向）基准测试，未涉及训练。

## 5. 实验数量与充分性

- **实验数量**：丰富多样，包括：
  - 3个MoE模型上的端到端和MoE层延迟对比（与4个SOTA系统）。
  - 单层微基准测试改变专家数（4~64）。
  - 算子级和MoE层级的加速比对比，涵盖3种路由器模式、2种GEMM形状、多种专家数、2种段大小。
  - SM资源竞争实验：消费者CTA从2到24共12个级别。
  - 正确性验证：1440个配置检查，覆盖所有组合。
  - padding开销分析。
- **充分性**：实验覆盖了主要变量（模型规模、负载分布、资源分配、通信粒度），对比了主流SOTA系统和朴素基线，结论具有说服力。但仅测试了4-GPU单节点场景，未涉及更大规模或多节点。

## 6. 论文的主要结论与发现

- 方法在三个MoE模型上端到端加速比达1.57x~2.64x（与FasterMoE相比），MoE层加速比达1.77x~2.74x。
- 相比传统无重叠基线，算子级加速比最高达3.01x，MoE层加速比最高达1.78x。
- 重叠比例持续保持在71.9%~99.9%，说明细粒度重叠能有效隐藏通信。
- 资源分配需平衡：消费者SM过少（如2个）导致通信积压，性能反而下降；适中的消费者SM（10~20）效果最佳。路由器偏斜会加剧资源竞争。
- 正确性验证表明输出差异在FP16舍入噪声范围内（最大相对误差1.9×10⁻³）。

## 7. 优点

- **非侵入式**：不需要修改底层GEMM算子或通信原语，依赖CUTLASS和NVSHMEM的现有接口，易于在现有框架中集成。
- **细粒度重叠**：分片级信令使得通信可以尽早开始，且保留tensor core效率，避免分解方法的小分片效率损失和主机同步。
- **鲁棒性**：对各种GEMM形状、路由器模式、专家数量均能稳定提升性能，且正确性有保证。
- **可调性**：消费者SM数量和段大小可调，便于针对不同负载和硬件配置优化。
- **效率高**：持久化内核消除了重复启动开销，且通过远程优先调度最大化重叠窗口。

## 8. 不足与局限

- **仅支持前向推理**：论文明确限定于前向传播，反向传播（训练）的支持列为未来工作。
- **仅测试4-GPU单节点**：未验证在更大规模集群或多节点、不同互联（如IB）下的性能。重叠收益可能受网络拓扑和带宽影响。
- **资源分配敏感**：消费者SM数量需要手动调优，过度分配或不足均会导致性能下降。文中未提供自适应选择机制。
- **padding计算开销**：远程拥有者对齐布局引入的零填充行会增加无用计算，尤其在偏斜路由下开销更大。文中虽有分析，但未评估极端偏斜下的实际影响。
- **仅对比了MoE专用系统**：与更通用的方案（如TensorFlow/PyTorch原生实现）的比较缺失。
- **缺乏能量效率分析**：未讨论GPU功耗或每瓦性能。

（完）
