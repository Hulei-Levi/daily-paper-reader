---
title: "MGDT: MLLM-Guided Diffusion Transformer with Relation-Adaptive Mixture-of-Experts for Multimodal Knowledge Graph Completion"
title_zh: MGDT：面向多模态知识图谱补全的MLLM引导扩散Transformer与关系自适应混合专家模型
authors: "Xu Hou, Meiyu Liang, Wei Huang, Yawen Li, Zhe Xue, Wu Liu, Guanhua Ye, Lei Shi, Kangkang Lu"
date: 2026-07-17
pdf: "https://arxiv.org/pdf/2607.15592v1"
tags: ["query:moe-special"]
score: 8.0
evidence: 使用混合专家与关系自适应路由进行多模态知识图谱补全
tldr: 多模态知识图谱补全中，现有扩散方法直接在原始多模态特征上降噪，需同时完成关系选择、跨模态对齐和实体生成，引入噪声和语义不一致。本文提出MGDT框架，采用先对齐后扩散的范式：通过关系自适应专家混合模块选择相关模态路径，用冻结多模态大语言模型对齐表示，再由图扩散Transformer生成实体。在三个基准数据集上取得最优性能，验证了该范式的有效性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.15592v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 874, \"height\": 786, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.15592v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 882, \"height\": 612, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.15592v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1654, \"height\": 883, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.15592v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 878, \"height\": 554, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.15592v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 869, \"height\": 654, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.15592v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 894, \"height\": 503, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.15592v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1851, \"height\": 1134, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.15592v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 809, \"height\": 458, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.15592v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 758, \"height\": 242, \"label\": \"Table\"}]"
motivation: 现有扩散方法在原始多模态特征上直接降噪，需同时处理关系选择、跨模态对齐和实体生成，导致条件噪声和语义不一致，性能受限。
method: 提出MGDT框架：RASR-MoE选择关系相关模态路径，冻结MLLM对齐多模态表示到统一空间，KGDT在图条件下生成缺失实体。
result: 在三个基准数据集上均超越所有强基线，证明对齐后扩散范式的有效性。
conclusion: 先对齐后扩散的范式有效抑制模态干扰和语义异质性，显著提升多模态知识图谱补全性能。
---

## 摘要
多模态知识图谱补全（MKGC）需要从结构、文本和视觉线索中推断缺失实体。现有的基于扩散的MKGC方法通常直接在原始多模态特征上进行去噪。这种设计迫使去噪器同时执行关系相关的线索选择、跨模态语义对齐和结构感知的实体生成，从而为扩散引入噪声和语义不一致的条件，导致补全性能次优。为解决这一局限性，我们提出MGDT：一种基于先对齐后扩散范式的新型MKGC框架——MLLM引导扩散Transformer与关系自适应混合专家模型。MGDT首先采用关系自适应语义路由混合专家模块，选择关系相关的多模态语义转换路径并抑制无关模态干扰；然后使用冻结的多模态大语言模型作为语义锚点，将路由后的多模态表示对齐到统一的潜在空间，减少跨模态语义异质性；最后，知识图谱扩散Transformer在对齐空间中进行图条件去噪生成，产生缺失实体表示。在三个基准数据集上的实验表明，MGDT始终优于强基线。

## Abstract
Multimodal Knowledge Graph Completion (MKGC) requires inferring missing entities from structural, textual, and visual cues. Existing diffusion-based MKGC methods usually denoise directly on raw multimodal features. Such a design forces the denoiser to simultaneously perform relation-dependent cue selection, cross-modal semantic alignment, and structure-aware entity generation, which introduces noisy and semantically inconsistent conditions for diffusion and consequently leads to suboptimal completion performance. To address this limitation, we propose MGDT: MLLM-Guided Diffusion Transformer with Relation-Adaptive Mixture-of-Experts (MGDT), a novel MKGC framework built on an align-then-diffuse paradigm. MGDT first employs a Relation-Adaptive Semantic Routing Mixture-of-Experts (RASR-MoE) module to select relation-relevant multimodal semantic transformation paths and suppress irrelevant modality interference. MGDT then uses a frozen Multimodal Large Language Model (MLLM) as a semantic anchor to align the routed multimodal representations into a unified latent space and reduce cross-modal semantic heterogeneity. Finally, a Knowledge Graph Diffusion Transformer (KGDT) performs graph-conditioned denoising generation in the aligned space to produce the missing entity representation. Experiments on three benchmark datasets show that MGDT consistently outperforms strong baselines.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：多模态知识图谱补全（MKGC）需要从结构、文本和视觉三种模态信息中推断缺失实体。现有基于扩散模型的方法直接在原始多模态特征上进行去噪，导致去噪器必须同时处理三件事：关系相关的模态线索选择、跨模态语义对齐、以及结构感知的实体生成。这种“多任务纠缠”引入噪声和语义不一致的条件，使得扩散生成质量下降，补全性能次优。
- **整体含义**：本文提出**先对齐后扩散（align-then-diffuse）** 的新范式，将多模态线索选择、跨模态语义校准和结构生成解耦成顺序流水线，从而在更干净、更统一的语义空间中进行扩散生成，显著提升MKGC性能。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：先通过关系自适应路由选择与当前关系最相关的模态信息，然后利用冻结的多模态大语言模型（MLLM）将路由后的多模态表示对齐到统一潜空间，最后在此对齐空间中进行图条件的扩散去噪生成。
- **关键技术细节**：
  - **RASR-MoE（关系自适应语义路由混合专家）**：对每个模态（结构、视觉、文本）维护一组专家。根据查询（头实体、关系）动态计算专家权重，并通过噪声扰动提高路由鲁棒性。路由后的模态表示通过可学习投影融合为统一实体表示。同时加入辅助分类损失保持判别能力，以及轻量CLUB正则化鼓励专家互补。
  - **MLLM锚点对齐**：使用冻结的Qwen3-VL（7B）作为语义锚点。将原始多模态特征拼接后投影到MLLM的输入维度，经过冻结的MLLM得到锚点隐藏状态，再投影回各模态空间。通过余弦相似度（对结构、视觉、文本分支）和温度缩放KL散度（对融合分支）将RASR-MoE的输出对齐到锚点，从而消除模态语义异质性。
  - **KGDT（知识图谱扩散Transformer）**：在训练中，条件由当前RASR-MoE输出（融合特征加时间嵌入）组成。扩散过程为标准DDPM前向加噪，反向去噪由Transformer（3个块）通过交叉注意力注入条件，预测噪声。训练目标包括L_moe、L_reg、L_align、L_diff四项之和。
- **算法流程**（文字说明）：
  1. 给定查询 (h, r, ?)，提取h的结构、视觉、文本特征。
  2. RASR-MoE根据r对各模态进行专家路由，得到路由后的模态表示并融合为e_f。
  3. 同时，原始特征经投影送入冻结MLLM得到锚点表示，通过对齐损失使e_f向锚点靠拢。
  4. 在训练中，对目标实体嵌入z0加噪得到z_t，条件c_t = e_f + τ_t，KGDT预测噪声，优化L_diff。
  5. 推理时，从高斯噪声开始，迭代去噪T步得到预测嵌入，与候选实体嵌入匹配排序。

## 3. 实验设计：数据集、基准、对比方法

- **数据集**：三个广泛使用的多模态知识图谱补全基准：
  - MKG-W、MKG-Y（来自Xu et al., 2022）
  - DB15K（来自Liu et al., 2019）  
  每个数据集包含结构三元组、文本描述和视觉属性。
- **评估指标**：MRR、Hits@1、Hits@3、Hits@10（标准过滤设置）。
- **对比方法**：共19个基线，分为：
  - **单模态KGC**：TransE、DistMult、ComplEx、RotatE、GC-OTE、TuckER。
  - **多模态KGC**：IKRL、TBKGC、TransAE、MMKRL、RSME、VBKGC、OTKGE、IMF、AdaMF-MAT、VISTA、MyGo、MoMoK、MCKGC（共13个，其中MCKGC为之前SOTA）。

## 4. 资源与算力

- 论文明确说明：使用单个NVIDIA V100 GPU（32GB显存）。
- 软件环境：PyTorch 2.6.0，优化器AdamW（学习率1e-4，权重衰减5e-5），批次大小128，最大训练轮数3000。
- 此外，使用BERT-base初始化文本特征、ResNet-50提取视觉特征、TuckER初始化结构嵌入。MLLM使用冻结的Qwen3-VL-7B。
- 未提及训练总时长（如小时数），但给出了模型规模和超参数。

## 5. 实验数量与充分性

- **实验数量**：共进行了四类实验：
  1. **主实验**：在三个数据集上与19个基线全面对比，报告所有指标。
  2. **消融实验**：在MKG-W上移除每种模态、移除RASR-MoE、移除语义锚点、替换KGDT为线性去噪器等共8个变体。
  3. **超参数敏感性分析**：对扩散步数、Transformer块数、MLLM对齐权重、扩散损失权重各绘制曲线。
  4. **MLLM泛化实验**：将锚点MLLM替换为CLIP-ViT-L/14和BLIP-2，与SOTA对比。
- **充分性评估**：实验设计比较全面，数据集覆盖典型规模，对比方法涵盖单模态和多模态主流方法，消融实验验证了每个组件的贡献，超参数分析找出了最优区间，MLLM泛化实验验证了范式不依赖特定MLLM。消融实验和超参数分析遵循学术规范，结果客观。略缺的是未在不同随机种子下报告方差，但趋势清晰。

## 6. 论文的主要结论与发现

- **主要结论**：MGDT在三个数据集上均取得最佳MRR，且Hits@1、Hits@3几乎全面领先，验证了“先对齐后扩散”范式在MKGC中的有效性。
- **关键发现**：
  - 移除RASR-MoE导致性能下降最严重（MRR从37.77降至30.88），表明关系自适应的模态路由至关重要。
  - 移除语义锚点后MRR降至33.45，说明MLLM引导的对齐能有效缓解跨模态异质性。
  - 使用Transformer去噪器优于线性去噪器（MRR 37.77 vs 36.68），证明深度条件建模的必要性。
  - 超参数分析表明，扩散步数100、Transformer块数3、对齐权重0.01、扩散权重0.1为最优平衡点。
  - MLLM替换实验显示即使替换为较弱模型（CLIP），仍能与SOTA持平，说明范式鲁棒可泛化。

## 7. 优点

- **方法设计创新**：提出“先对齐后扩散”解耦范式，思路清晰，有效解决现有方法的多任务纠缠问题。
- **组件设计精细**：RASR-MoE引入噪声扰动路由和专家互补正则化，增强鲁棒性；锚点对齐使用冻结大模型，既提供强先验又避免过拟合；KGDT用Transformer建模关系依赖。
- **实验充分客观**：在三个标准数据集上与19个基线对比，消融实验包含8个变体，超参数分析覆盖四个关键维度，MLLM泛化实验验证了通用性。结果具有说服力。
- **可复现性好**：明确给出了实现细节（框架、优化器、学习率、批次大小、模型超参数等）。

## 8. 不足与局限

- **计算资源受限**：仅使用单张V100 GPU，最大epoch 3000，未探索更大规模模型或更长的训练；MLLM锚点冻结无法微调，可能限制了下游对齐的质量。
- **消融实验未报告方差**：虽然趋势一致，但缺少多次运行的均值与标准差，难以判断统计显著性。
- **未分析长尾/稀有实体**：论文未讨论模型对低频实体或复杂关系（如多跳）的泛化能力。
- **模态信息假设**：假定每个实体均有完整的文本和视觉描述，实际场景中可能存在缺失模态，论文未探讨模态缺失下的鲁棒性。
- **对齐强度敏感性**：超参数分析显示对齐权重需精确调优（最优0.01），在实际应用中可能需要针对不同数据集或MLLM重新调整。
- **主要局限**：方法依赖于大型冻结MLLM（7B），推理时仍需加载该模型，对部署资源有一定要求；论文未与更轻量的对齐方法（如对比学习）进行对比，以验证MLLM的不可替代性。

（完）
