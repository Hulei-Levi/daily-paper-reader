---
title: "Half the Experts, All the Code: One-Shot Domain Pruning of Mixture-of-Experts LLMs for Coding"
title_zh: 一半专家，全部代码：面向编码的混合专家大语言模型的一次性领域剪枝
authors: Anik Jha
date: 2026-07-18
pdf: "https://arxiv.org/pdf/2607.16721v1"
tags: ["query:moe-special"]
score: 10.0
evidence: 从MoE大语言模型中剪枝专家以专注于编程任务，直接研究专家专业化程度
tldr: MoE代码大模型因专家池庞大难以部署，但大多数专家对用户无用。本文研究如何移除一半专家而不损失代码能力，通过五种策略修剪两个MoE模型，发现一半专家可安全移除，代码基准无显著下降，但最佳修剪策略因模型而异。研究揭示困惑度指标不可靠，轻量微调可恢复部分损失，且修剪仅在量化低于3比特时胜出。贡献在于提出需按实际任务逐模型验证的专家修剪方法论。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.16721v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1713, \"height\": 145, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.16721v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1024, \"height\": 1013, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.16721v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1589, \"height\": 606, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.16721v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1323, \"height\": 436, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.16721v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 832, \"height\": 327, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.16721v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 896, \"height\": 435, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.16721v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1003, \"height\": 456, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.16721v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1009, \"height\": 434, \"label\": \"Table\"}]"
motivation: 移除MoE代码模型中大量冗余专家以缩小模型尺寸，同时保持代码能力。
method: 对两种MoE模型使用五种专家选择策略进行一半专家修剪，并在代码基准上评估。
result: 一半专家可移除且不损代码性能，但最优策略因模型而异；困惑度可能误导；修剪优于极低比特量化。
conclusion: 专家修剪需针对每个模型和实际任务进行验证，不能跨模型推广。
---

## 摘要
最强的开源编码模型是混合专家（MoE）网络：它们的大部分规模来自大量"专家"子网络池，每个token仅激活其中少数几个。这个池正是这些模型无法适配大多数开发者自有机器的主因，然而对于仅需编码帮助的用户而言，大多数专家编码的能力永远不会被调用。我们探究可以从两个近期不同家族的开源MoE模型（Qwen3.6-35B-A3B和Gemma-4-26B-A4B）中移除多少个专家以及移除哪些专家，采用五种选择策略，并以用户的方式评判：模型是否仍能写出正确代码。对于任一模型，移除一半的专家在主代码基准测试上均未产生统计上可检测的损失，而损伤几乎完全落在编码之外的其它能力上，这正是预期的权衡。但获胜策略在两个模型之间反转，因此在某一族模型上验证有效的方案不能假定适用于另一族。我们进一步表明，大量剪枝文献所依赖的困惑度指标可能将一个损坏的模型排在完整模型之上；轻量级微调可以恢复激进攻剪枝所损失的大约一半性能；而与将完整模型量化到相同内存相比，剪枝仅在量化需降至每权重低于3比特时才胜出。五次试图推翻这一交叉点的尝试（预先设定失败标准：更好的校准、有保护的选取、因果专家重要性、失败归因以及让每个模型通过执行反馈修复失败的智能体评估）均未能动摇该结论；最后一次尝试表明，单次基准测试普遍高估了压缩惩罚，因为一次修复回合完全消除了2比特量化的惩罚。专家剪枝有效，但要求在模型实际服务的任务上进行逐模型验证。

## Abstract
The strongest open-weight coding models are mixture-of-experts (MoE) networks: most of their size comes from large pools of "expert" subnetworks, of which only a few act on any token. That pool is why these models do not fit on the machines most developers own, yet for a user who only wants coding help, most experts encode abilities that will never be invoked. We ask how many experts can be removed, and which, by pruning two recent open-weight MoE models from different families (Qwen3.6-35B-A3B and Gemma-4-26B-A4B) under five selection strategies, judged the way a user would: by whether the model still writes correct code. Half the experts can be removed from either model with no statistically detectable loss on the primary code benchmark, and the damage lands almost entirely on abilities outside coding, the intended trade. But the winning strategy flips between the two models, so a recipe validated on one family cannot be assumed to work on another. We further show that perplexity, the metric much of the pruning literature leans on, can rate a broken model above an intact one; that a lightweight fine-tune recovers about half of what aggressive pruning loses; and that against quantizing the full model to the same memory, pruning wins only where quantization would have to drop below 3 bits per weight. Five attempts to overturn that crossover, with failure criteria fixed in advance (better calibration, guarded selection, causal expert importance, failure attribution, and an agentic evaluation letting each model repair its failures from execution feedback), all leave it standing; the last shows single-shot benchmarks overstate compression penalties broadly, as one repair turn erases the 2-bit quantization penalty entirely. Expert pruning works, but it demands per-model validation on the task the model will actually serve.

---

## 论文详细总结（自动生成）

# 详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：当前最强的开源代码生成模型多是混合专家（MoE）架构，其总参数量巨大（如35B），但每个token只激活少量专家（如3B）。庞大的专家池导致模型无法适配大多数开发者拥有的个人设备，而纯代码用户实际上只需要编码能力，大多数专家从未被调用。论文探究能否通过一次性（one-shot）剪枝移除大量专家，只保留编码必需的部分，从而缩小模型而不损失代码生成能力。
- **整体含义**：验证了“一半专家可安全移除”的假设在两个模型家族上均成立，但最佳剪枝策略不可迁移；揭示了困惑度作为评估指标的误导性；并界定了剪枝 vs 量化的适用场景。

## 2. 论文提出的方法论
- **核心思想**：基于校准数据（编码为主）收集每个专家的路由和激活统计量，使用某种重要性评分对专家排序，然后直接从检查点删除低分专家（以及路由器对应行），不重新训练（one-shot）。剪枝后可选择性地用蒸馏微调恢复部分损失。
- **关键技术细节**：
  - **校准**：使用2,047条聊天样本（70%编码相关，30%通用），前向传播一次，收集每个（层, 专家）的：路由token数、门控质量（gate mass）、全softmax概率和、激活范数的一阶/二阶矩、REAP统计量（\(g_{\ell,e}(x)\|f_{\ell,e}(x)\|_2\)）。
  - **五种选择策略**：
    1. **REAP**：加权门控范数（\( \frac{1}{|D|} \sum g_{\ell,e}(x) \|f_{\ell,e}(x)\|_2 \)）。
    2. **路由质量（Routing mass）**：仅使用门控概率和。
    3. **平均激活范数（MAN）**：激活范数的均值。
    4. **对比评分（Domain-contrastive）**：\( S_{\ell,e} = F_{\ell,e}(T) / (F_{\ell,e}(R) + \epsilon) \)，其中 \(F_{\ell,e}\) 是目标域/参考域的REAP统计量，旨在选出编码专属专家。
    5. **随机选择**（基线）。
  - **手术过程**：专家存储为融合的三维参数，剪枝直接沿首维进行索引选择，同时裁剪路由器矩阵的行；无需实例化模型，峰值内存仅一个张量；验证幸存字节与原始一致。
  - **治愈（Healing）**：对激进攻剪枝（25%保持），用基础模型在相同校准提示上生成的回答进行蒸馏：路由专家冻结，只微调注意力层、线性注意力投影、共享专家（LoRA rank 64）和全量训练路由器矩阵；训练不到3小时。
  - **导出**：转换为GGUF格式并量化（如Q4_K_M等），通过真实硬件验证。
- **公式**：核心是对比评分公式 \( S_{\ell,e} = F_{\ell,e}(T) / F_{\ell,e}(R) \)，及其变体。文中还提到一种幅度门控混合（\(S = F_T^2 / (F_T + F_R)\)）用于消融。

## 3. 实验设计
- **数据集与场景**：
  - **代码生成基准**：HumanEval+（164题）和MBPP+（378题），均为Python问题，pass@1（贪心解码，禁用推理模式）。
  - **校准集**：2,047条聊天样本（Evol-CodeAlpaca, Hermes函数调用, SWE-smith轨迹, Tulu-3通用样本），共3.15M tokens。
  - **评估还包括**：固定8提示烟测（检测退化）、困惑度（编码/通用保留集各256样本）、记忆和吞吐量测量。
- **对比的方法**：
  - **不同专家选择策略**：REAP、路由质量、MAN、对比评分、随机（多重种子）。
  - **不同保持比例**：75%、50%、37.5%、25%的专家保留（每层均匀）。
  - **剪枝 vs 量化**：50%剪枝后量化到不同比特（Q4、Q5、Q8） vs 完整模型量化到相同内存（IQ2_M, Q2_K, Q3_K_M, Q4_K_M）。
  - **治愈实验**：25%保持模型经蒸馏微调后对比基线。
  - **Agentic评估**：每个模型有一次修复回合（展示失败测试输入和预期输出）。
  - **因果重要性测量**：使用2,000对反事实掩蔽估计专家重要性。
- **跨家族复制**：在两个模型（Qwen3.6-35B-A3B 和 Gemma-4-26B-A4B）上执行完全相同的协议。

## 4. 资源与算力
- **硬件**：单台NVIDIA DGX Spark（GB10），119 GB统一内存，约273 GB/s带宽。
- **训练**：治愈微调在目标设备上运行 **不到3小时**。
- **标注**：论文未明确给出总GPU小时数或详细训练资源，强调所有测评均在同一设备上完成。

## 5. 实验数量与充分性
- **实验组数**：
  - 主要实验：2个模型 × 5种策略 × 4个保持比例 = 40+组合（部分比例只测最佳策略）。
  - 量化对比：5种量化级别 vs 3种剪枝量化组合。
  - 额外实验：治愈、Agentic评估、因果重要性、剂量响应（对比评分混合）、内存/吞吐测量。
  - 随机选择使用多粒种子（3个）报告均值和标准差。
  - 所有比较报告95%配对bootstrap置信区间，部分使用McNemar检验。
- **充分性评价**：实验设计全面，覆盖多个角度，跨家族复制增强了结论一般性。但仅限两个模型家族，且校准集仅单次混合。

## 6. 论文的主要结论与发现
- **核心结论**：从通用MoE模型中移除一半专家，以编码为中心校准后，HumanEval+性能 **无统计显著损失**（Qwen上）；但不同模型的最佳选择策略不同——REAP在Qwen上夺冠，而在Gemma上却大幅落后路由质量（相差12.8点）；因此 **不能跨模型家族推广任意一种剪枝准则**。
- **随机选择完全失败**：在两个模型上随机剪枝导致代码能力崩溃（如HumanEval+从0.89降至0.311甚至更低）。
- **对比评分效果差**：纯对比评分（只保留编码专属专家）摧毁模型，因为剔除了编码也需要的共享高流专家；幅度信号（如REAP）才是关键。
- **困惑度不可靠**：随机剪枝后的模型困惑度居然优于完整模型（4.82 vs 5.38），但pass@1低了58点；Gemma的对话模板使困惑度膨胀14倍，跨家族比较无意义。
- **剪枝 vs 量化**：在相同内存预算下，只有当完整模型量化必须降至 **低于3比特**时，剪枝才显著胜出（如50%剪枝+Q5在13.3GB上优于IQ2_M的完整模型）；否则量化是更安全的选择。
- **治愈效果**：25%保持模型经LoRA微调（只改路由和共享层，专家冻结）恢复HumanEval+损失的52%和MBPP+损失的23%；这与翻译域文献声称的“参数高效恢复不够”相反。
- **Agentic评估**：一次修复回合补偿了压缩带来的单次惩罚（尤其是2比特量化完全修复），但剪枝与量化的相对排序不变。

## 7. 优点
- **双家族跨模型验证**：在同一协议下评估两个设计迥异的MoE模型，增加了结论可靠性。
- **统计严谨**：所有比较均报告95%配对bootstrap置信区间，部分签署McNemar检验。
- **揭示困惑度的误导性**：通过具体实例（剪枝模型困惑度更低但pass@1远差）有力抨击了剪枝文献依赖困惑度的做法。
- **全面的实际部署比较**：同时考虑了量化、剪枝、微调恢复和吞吐量，并给出实际硬件结果。
- **提出并分析对比评分**：虽未成功，但系统展示了为何幅度信号重要，域信息只能补充不能替代。
- **Agentic评估**：指出单次基准测试可能高估压缩损失，为模型评价提供更实际视角。

## 8. 不足与局限
- **模型家族有限**：仅两个模型，第三个家族可能又有不同，结论无法自动推广。
- **校准集单一**：只使用一次特定混合（70/30编码/通用），未系统探索不同校准分布的影响。
- **代码基准局限**：只评估Python代码（HumanEval+/MBPP+），未覆盖多语言或更复杂的软件工程任务。
- **硬件局限**：所有测量只在单一设备（DGX Spark）上进行，绝对吞吐量不能迁移。
- **治愈实验仅在一个模型上运行**（Qwen），通用性未知。
- **无法处理超内存模型**：当前方案需要模型完整加载以收集统计量，不能直接对数百亿参数模型进行流式剪枝。
- **对话模板问题**：Gemma的模板使困惑度测量困难，论文使用bare content解决，但无法完全消除影响。
- **均匀每层容量**：为简化与兼容，每层保持相同专家数；动态分配可能提升效果。
- **通用文本损失评估粗糙**：仅用困惑度衡量，未深入测试聊天或推理能力（但作者声明这些不是目标）。

（完）
