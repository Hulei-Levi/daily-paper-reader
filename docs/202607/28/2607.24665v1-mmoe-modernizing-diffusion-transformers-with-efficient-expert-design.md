---
title: "MMOE: Modernizing Diffusion Transformers with Efficient Expert Design"
title_zh: MMOE：用高效的专家设计现代化扩散变换器
authors: "Yanhao Jia, Jiepeng Wang, Haibin Huang, Chi Zhang, Erik Cambria, Xuelong Li"
date: 2026-07-27
pdf: "https://arxiv.org/pdf/2607.24665v1"
tags: ["query:moe-special"]
score: 9.0
evidence: 用稀疏MoE和高效专家路由设计现代化扩散变换器
tldr: 大语言模型通过效率机制实现规模扩展，但扩散Transformer的MoE设计仅增加参数量忽视效率。本文提出ModernMOE，系统集成路由/共享/轻量专家、门控残差路由与注意力残差复用。在单节点八卡H100预算下，MMOE每步收敛更快、FID更低，达到最佳质量-成本平衡。该工作证明AIGC基础模型可借鉴LLM效率设计实现平衡扩展，而非单纯扩大参数。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.24665v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1757, \"height\": 1382, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.24665v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 910, \"height\": 673, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.24665v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 904, \"height\": 699, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.24665v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 911, \"height\": 677, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.24665v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1810, \"height\": 1712, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.24665v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1056, \"height\": 251, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.24665v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1167, \"height\": 423, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.24665v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 356, \"height\": 199, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.24665v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 784, \"height\": 131, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.24665v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 786, \"height\": 129, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.24665v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 517, \"height\": 166, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.24665v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 634, \"height\": 132, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.24665v1/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 563, \"height\": 270, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.24665v1/table-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 557, \"height\": 164, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.24665v1/table-010.webp\", \"caption\": \"\", \"page\": 0, \"index\": 10, \"width\": 1261, \"height\": 215, \"label\": \"Table\"}]"
motivation: 现有扩散Transformer稀疏专家设计仅扩大参数量，缺乏LLM中控制成本的效率机制。
method: 将路由专家、共享轻量专家、门控残差路由及注意力残差复用系统集成到SiT风格扩散Transformer中。
result: 在单节点八卡H100预算下，MMOE每步收敛更快，FID低于密集和中间稀疏基线，且质量-成本平衡最优。
conclusion: 结果表明AFMs可通过引入被验证的效率设计而非单纯增加参数，实现类似LLM的平衡扩展。
---

## 摘要
现代大语言模型通过将容量增长与效率相结合，在容量增长的同时控制每次token的成本和部署成本，从而成功扩展。AIGC基础模型（AFMs），特别是扩散变换器骨干网络，已经开始采用稀疏专家，但最近的研究主要集中在增加总参数数量和稀疏率，而没有引入使LLM扩展变得实用的效率机制，因此生成质量很少与训练和部署成本相平衡。这提出了一个自然的问题：高效LLM扩展背后的架构原则能否以更平衡的方式适应AFMs？我们引入了ModernMOE（MMOE），这是对SiT风格扩散变换器的现代化改造，系统地将路由专家、共享专家和轻量级专家、门控残差路由以及注意力残差信息重用适配到AIGC生成中。MMOE不是将MoE视为单一的即插即用替代方案，而是研究不同的现代专家组件在扩散变换器内部组合时如何影响收敛、效率和生成质量。本文中的每个实验都在单个八GPU H100节点上训练，批量大小为256，训练400k步，这是一个可访问的单台机器预算。在匹配的训练和采样协议下，在此预算下，MMOE在每个记录检查点都达到了更低的FID，即每个训练步骤收敛更快，优于密集和中间稀疏专家基线，并且在稀疏变体中达到了最佳的质量-成本平衡。路由分析进一步显示了跨深度的稳定专家专业化、轻量级路由的大量使用，以及在去噪过程中逐步路由变化的适度性。这些结果表明，AFMs可以通过引入经过验证的效率设计，而不是仅仅增加总参数和稀疏率，来遵循LLM的平衡扩展路径。

## Abstract
Modern large language models scale successfully by pairing capacity growth with efficiency, keeping per-token and deployment costs under control as capacity grows. AIGC Foundation Models (AFMs), especially diffusion-transformer backbones, have begun to adopt sparse experts, but recent efforts mostly enlarge total parameter counts and sparsity ratios without importing the efficiency mechanisms that made LLM scaling practical, so generation quality is seldom balanced against training and deployment cost. This raises a natural question: can the architectural principles behind efficient LLM scaling be adapted to AFMs in a more balanced way? We introduce ModernMOE (MMOE), a modernization of SiT-style diffusion transformers that systematically adapts routed experts, shared and lightweight experts, gate-residual routing, and attention-residual information reuse to AIGC generation. Rather than treating MoE as a single plug-in replacement, MMOE studies how different modern expert components affect convergence, efficiency, and generation quality when composed inside a diffusion transformer. Every experiment in this paper is trained on a single eight-GPU H100 node with batch size 256 for 400k steps, an accessible single-machine budget. Under matched training and sampling protocols and at this budget, MMOE reaches lower FID at every recorded checkpoint, that is, it converges faster per training step, than dense and intermediate sparse-expert baselines, and among the sparse variants it attains the best quality-cost balance. Routing analysis further shows stable expert specialization across depth, substantial use of lightweight routes, and modest step-to-step routing changes during denoising. These results suggest that AFMs can follow the balanced scaling path of LLMs by importing proven efficiency designs, rather than by simply increasing total parameters and sparsity ratios.

---

## 论文详细总结（自动生成）

好的，根据您提供的论文内容，以下是对《MMOE: Modernizing Diffusion Transformers with Efficient Expert Design》的详细中文总结。

## 论文总结

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：大语言模型（LLMs）通过引入稀疏专家（MoE）等效率机制，成功地在增加参数容量的同时控制了单次推理和部署成本。然而，AIGC基础模型（特别是扩散变换器）在采用MoE时，主要遵循了“扩大专家池和稀疏率”的路径，忽略了LLM中那些控制计算成本的成熟效率设计（如零计算专家、门控残差路由等）。这导致生成质量、训练成本和部署效率之间难以取得良好平衡。本文旨在探究：能否将LLM中经过验证的高效扩展架构原则，以更平衡的方式迁移到AIGC扩散变换器中？
- **整体含义**：本文主张，AIGC基础模型的扩展不应仅通过增加总参数数量和稀疏率来实现，而应借鉴LLM的“平衡扩展”路径，通过精细化的效率设计来提升质量与成本的权衡。作者将这种思路类比于ConvNeXt在视觉骨干网络中的现代化工作。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：提出**ModernMOE（MMOE）**，是一种对SiT风格扩散变换器的现代化改造。MMOE并非简单地将MoE作为密集前馈网络（FFN）的替代品，而是系统性地将LLM中四种关键的效率机制组合到一个统一的稀疏专家扩散变换器骨干中。
- **关键技术细节**：
    - **MoE++专家层**：专家池不全是标准MLP专家，还包含三种零计算专家：
        - **复制专家** (`f_copy(h) = h`)：直接返回输入。
        - **零专家** (`f_zero(h) = 0`)：返回零向量。
        - **常数专家** (`f_const,j(h)`)：学习输入与一个可学习常数向量的混合。
    - **门控残差路由**：路由决策不仅依赖当前token状态，还依赖来自上一个专家层的路由倾向。具体地，当前token的logits会加上一个经过学习线性变换的上一层的门控残差：`ℓ_i = g(h_i) + A * r_prev_i`。这使得路由决策在深度和时间上保持连贯。
    - **注意力残差信息复用**：在每个MMOE块中，注意力子层和MLP（MoE++）子层之前，都会对之前**已完成块组**的状态进行注意力残差聚合。聚合方式是将所有已完成块状态和当前部分状态堆叠，通过RMSNorm和可学习的伪查询向量进行软注意力加权求和，作为当前子层的输入。这实现了跨深度的信息重用。
- **算法流程**（文字描述）：
    1. 输入潜在特征`x`，时间步`t`，类别标签`y`。
    2. 初始化为块状态列表`C`（空）和部分状态`u`。
    3. 对于每个变换器块：
        a. 进行注意力子层计算：输入为`u`或注意力残差聚合后的结果。
        b. 进行MoE++子层计算：输入为`u`或注意力残差聚合后的结果，并使用门控残差路由进行专家选择（Top-2）。
        c. 每经过一个块组（例如，每半个块数），将当前块输出`u`追加到`C`中，并将`u`重置为零。

### 3. 实验设计

- **数据集与场景**：主要在**类条件 ImageNet-256** 上进行评估，并扩展到了 **ImageNet-512**。所有模型在潜在空间（使用Stable Diffusion风格的VAE）中训练。
- **Benchmark与指标**：主要指标是**FID（Fréchet Inception Distance）**，使用pytorch-fid计算。采用50步ODE采样、50K生成样本和分类器无关引导（CFG）作为默认协议。
- **对比方法**：
    - **内部对比（核心）**：作者设计了一条渐进式的现代化路径，所有变体均基于SiT骨干，仅在内部计算上修改：
        1.  **Dense SiT**：密集MLP基线。
        2.  **Standard MoE**：用路由MLP专家替换所有MLP块。
        3.  **SMOE**：在Standard MoE基础上，添加一个始终激活的共享专家。
        4.  **MoE++**：在SMOE基础上，引入零、复制、常数专家及门控残差路由。
        5.  **AMOE**：在Standard MoE基础上，引入注意力残差聚合。
        6.  **MMOE**：结合MoE++和注意力残差聚合（最终方案）。
    - **与外部方法对比（非直接可比）**：表II中列出了已发表的扩散MoE方法（如DiT-MoE, Race-DiT等），但明确指出由于训练预算和协议不同，这些结果不能直接比较。

### 4. 资源与算力

- **算力**：所有实验均在**单个八GPU H100节点**上完成，批量大小为**256**。
- **训练时长**：模型训练**400k个优化步骤**。每个变体的具体训练时间不同，例如Dense SiT为23小时，MMOE为67小时（AMOE则需120小时）。
- **总结**：文中强调了这一预算的战略意义——在一个可访问的单机预算下进行全部现代化研究，而非依赖大规模集群。

### 5. 实验数量与充分性

- **实验数量**：论文进行了较为充分的实验，包括：
    - **主现代化路径对比**：表I展示了6种变体在200k、300k、400k步时的FID。
    - **模型规模缩放性**：在S/2、B/2、L/2等不同规模下比较Dense SiT和MMOE（表III）。
    - **种子鲁棒性**：对三个随机种子进行了测试（表V），验证了排名稳定性。
    - **超参数敏感性**：探究了样本数量（10k-50k）、采样步数（50-250步）和引导尺度（CFG 1.0 vs 1.5）对FID的影响（表VII, VIII, IX）。
    - **路由分析**：定量分析了专家负载、轻量级专家使用率、路由切换率等（表X）。
    - **内存分析**：比较了AMOE和MMOE的单块激活内存（图4）。
- **充分性与公平性**：
    - **充分性**：实验设计较为全面，覆盖了主要架构变体、模型规模、关键超参数和内部机制分析。
    - **公平性**：内部对比非常公平，所有变体共享相同的SiT训练接口、数据集预处理、目标函数和采样器。作者明确承认了与外部方法不可直接比较，体现了严谨性。关于消融研究，主现代化路径本身就是一个自然的消融序列，每个组件的作用可以清楚分离。

### 6. 论文的主要结论与发现

- **主要结论**：MMOE通过整合LLM中的效率设计，在较小的单节点训练预算下，实现了比密集SiT和中间MoE变体**更快收敛、更低FID**，达到了最佳的质量-成本平衡。
- **具体发现**：
    - 稀疏专家容量本身有益，但更大的增益来自**轻量级路由**和**注意力残差聚合**。
    - 注意力残差聚合（AMOE）提供了最大幅度的单步性能提升。
    - 轻量级专家（MoE++）有效降低了训练内存和计算成本，尤其是在早期和中间块（轻量级路由使用率高达44.6%），且不牺牲最终性能。
    - 门控残差路由导致了**稳定的块级专家专业化**和**低的相邻去噪步间路由切换率**（约1.4%-2.7%）。
    - MMOE的现代化改造在S、B、L和XL多种模型规模下均有效。

### 7. 优点：方法或实验设计上的亮点

- **方法亮点**：
    - **“现代化”的视角**：借鉴ConvNeXt思路，将问题框架为“如何用LLM的成熟设计现代化扩散变换器”，非常清晰且有价值。
    - **模块化、渐进式设计**：不是提出一个“大而全”的黑盒，而是逐步引入每个组件，允许清晰归因每个机制的效果。
    - **高效的效率设计组合**：成功将门控残差路由、注意力残差复用和零计算专家结合在一起，在性能和成本之间取得了很好的平衡。
- **实验设计亮点**：
    - **严格的内部对比**：所有变量在完全相同的代码库、训练计划和评估协议下进行，对比结果高度可信。
    - **自包含的算力预算**：强调在单节点8卡H100上的训练，证明了方法在有限资源下的实用性和可复现性。
    - **全面的分析**：不仅报告了FID指标，还进行了详细的专家路由分析和内存分析，深入解释了性能提升的来源。

### 8. 不足与局限

- **实验覆盖有限**：
    - 主要实验限于类条件ImageNet（256和512），并未在更具挑战性的“文本到图像”任务上进行验证。
    - 所有内部对比只在SiT骨干上进行，未在DiT等其他扩散变换器骨干上验证其通用性。
- **直接可比性缺失**：与现有最先进的扩散MoE方法（如DiT-MoE）的对比仅能引用其报告数字，由于训练预算和协议的巨大差异，无法得出强弱结论（作者自己承认这一点）。
- **采样步数敏感性未完全解释**：表VIII显示，对于MMOE-XL/2检查点，增加ODE步数至250步并未改善FID，这有些反直觉，作者也将其归因于与引导尺度或小样本数估计的混淆，而非一个稳健的结论。
- **系统开销分析不完整**：论文指出多GPU训练是通讯受限的，当前跟踪的训练时间可能不是计算受限下的最优表现。同时，仅分析了单块内存和总训练时间，对稀疏模型带来的分布式通信、负载均衡等系统开销带来的实际训练复杂性剖析不够深入。
- **路由分析的局限性**：路由分析仅在**收敛后的XL检查点、50步无引导**协议下进行，未扩展到其他模型规模、引导尺度和训练阶段，其泛化性未知。

（完）
